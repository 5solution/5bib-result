# 5BIB Result — Feature Log

> **Owner:** 5bib-manager
> **Append-only.** Mỗi `/5bib-deploy` thêm 1 dòng vào BẢNG dưới (mới nhất ở trên cùng).

---

## 📈 Counter

**Next FEATURE-XXX:** `FEATURE-043` _(F-037 V2 ✅ DEPLOYED 2026-05-18 code-level — on-sale-race-detail-page resolves TD-F036-09 (F-037 number REUSED from V1 DOCX colspan deployed 2026-05-15 — collision documented, hardening note recorded). F-039 still INITIATED 2026-05-15 Analytics Per-Event & Per-Day Enhancement — defer pending. F-040 ✅ DEPLOYED 2026-05-17 release/v1.8.5. F-041 ✅ DEPLOYED 2026-05-18 release/v1.8.6 — GA4 Analytics. F-042 ✅ DEPLOYED 2026-05-18 release/v1.8.7 — Contract DOCX + BBNT Financial Bug Fix HIGH SEVERITY legal/finance risk RESOLVED.)_

> _(2026-05-18 15:34 ICT FEATURE-037 V2 ✅ PROD LIVE release/v1.8.8 commit ec3cd07 — PROD verified: raceId 104 MuCangChai/172 Cuc Phuong/174 Phong Nha/179 Trang An all return 200 + valid DTO, p95 124ms (target <500ms cold ✅, median 39ms warm exceeds <100ms target ✅), FE page `result.5bib.com/giai-chay/104` 200 OK 55KB rich content + SEO `<title>` + CTA `5bib.com/vi/events/104_104?ref=seo-giai-chay&utm_*` embedded. TD-F037-01 ✅ RESOLVED. TD-F036-09 ✅ RESOLVED. 2 NEW TD discovered post-deploy: TD-F037-SITEMAP-ISR-COLD LOW (sitemap ISR 24h cache returned empty on cold deploy — will self-heal at next tick OR via redeploy/revalidate) + TD-F037-VERCEL-REWRITE-5TICKET MED SEO (5Ticket team needs Vercel rewrite `5bib.com/giai-chay/*` → `result.5bib.com/giai-chay/*` — same TD-F027-PHASE2-16 pattern). CI 4 runs ✅: DEV main 26043161157 (6m10s), PROD release/v1.8.8 26043204847 (7m52s) + 26043277602 docs sync (6m21s), DEV docs 26043291052 (11s).)_

> _(2026-05-18 FEATURE-037 (V2 on-sale-race-detail-page) ✅ DEPLOYED code-level — Promo Hub on-sale internal SEO detail page resolves TD-F036-09. EXTEND_EXISTING F-027/F-033/F-036. Workflow FULL 5-gate (Init→PRD→Plan→Code→QC→Deploy) clean. **Number collision:** F-037 reused — V1 = DOCX colspan widths deployed 2026-05-15 release/v1.8.1, V2 = this feature (different scope same folder slot). Resolution: keep both in log, distinguish via "(V2 on-sale-race-detail-page)" label, document hardening note in 05-manager-deploy.md (counter bump immediately on /5bib-init, not at deploy). 13 files (9 BE + 4 FE). Backend: NEW entity `OnSaleCourseReadonly` 16 cols mapping table `race_course` (RENAMED from initial `RaceCourseReadonly` to avoid TS identifier collision with existing race-master-data/RaceCourseReadonly 3-col entity — TypeORM supports multi-entity-per-table via different class names) + extended `RaceReadonly` +8 cols (description/images/eventType/raceType/district/season/locationUrl/province) + NEW `RaceOnSaleDetailDto` (22 fields) + `RaceCourseDto` (17 fields) + `findRaceOnSaleByUrlName(urlName)` service method (~70 lines, OR `url_name=:urlName OR race_id=:raceId` with `^\d+$` numeric regex safe parse, parameterized TypeORM, bit field CAST pattern, Redis cache `promo-hub:race-on-sale-detail:<urlName>` TTL 600s) + NEW endpoint `GET /api/promo-hubs/races-on-sale/by-url-name/:urlName` (public, no auth same as F-033 list, 200/404 status codes, NotFoundException with VN message) + 10 NEW TC-37-XX tests. Module wiring BOTH `forFeature` + `forRoot(entities[])` per F-033 critical lesson. Frontend: REWRITE `getRaceBySlug()` dual-source MongoDB-first → MySQL on-sale fallback → null cascade (BR-37-07 prevents flicker during race transition BÁN VÉ→VẬN HÀNH), REVERT `RaceCard.tsx` (all sources now internal `<Link href="/giai-chay/${slug}">` — F-036 regression risk mitigated, verified 17 on-sale cards live preview), extended `RaceCTA.tsx` BR-37-11 registration-closed disabled state, extended `sitemap-races.xml/route.ts` to include 17 on-sale slugs priority 0.9 (results URL still skip for on-sale — no /ket-qua page). 35/35 tests PASS (10 new TC-37 + 25 F-027/F-033 regression). TypeScript `pnpm tsc --noEmit` exit 0 BE+FE. Selling-web URL format per BR-12: `5bib.com/vi/events/{slug}_{raceId}?ref=seo-giai-chay&utm_*`. Anti-pattern enforcement: ZERO `<form>` mua vé, ZERO `<button onClick>` purchase. QC ✅ APPROVED WITH CAVEATS — 6 phases pass, 0 CRITICAL/HIGH security threats (10 vectors reviewed), 2 MEDIUM deferred post-deploy (T2 XSS sanitization frontend-side, T9 perf SLA measure). 11 TD-F037-* tracked (6 Coder + 5 QC-added). Manager Code Review (skill MANDATE 2026-05-17) — 5 critical files spot-checked: promo-hub.service.ts (730-810) clean parameterized queries + Redis try/catch + numeric regex defense; promo-hub.controller.ts (94-128) route ordering correct + 404 not 500 + ThrottlerGuard inherited; on-sale-course-readonly.entity.ts naming collision resolved cleanly; seo-api.ts (318-355) dual-source flow with source marker + encodeURIComponent + safeFetch wrapper; RaceCard.tsx revert clean F-036 regression mitigated. 3 minor concerns (all tracked as TD or accepted): OR-condition double-index scan acceptable for O(1) LIMIT 1, hand-pick mapping mitigated by TC-37-10 22-field assertion, no draft fall-through log non-blocking. 0 red flags. **Patterns minted:** (1) "Dual-source race resolution" — MongoDB precedence over MySQL fallback for race transition handling; (2) "Multi-entity-per-table TypeORM" — when 2 modules need different col subsets from same table, name classes differently (RaceCourseReadonly vs OnSaleCourseReadonly). **TD-F036-09 ✅ RESOLVED** — on-sale internal detail page now exists. 4 caveats accepted: live endpoint verify deferred to release/v1.8.8 + curl checklist, frontend XSS sanitization verify post-deploy, perf SLA k6 measure post-deploy, Phase 6 persona live walkthrough deferred. **NOT YET pushed to remote** — Danny next: push `feat/F-037-on-sale-race-detail-page` → PR main → CI deploy DEV → cherry-pick release/v1.8.8 PROD → 6-item curl checklist.)_

> _(2026-05-18 FEATURE-042 ✅ DEPLOYED via release/v1.8.7 — Contract DOCX + BBNT Financial Number Bug Fix. BUGFIX HIGH SEVERITY (legal/finance accuracy). Danny PROD bug 2026-05-18: contract DOCX `6a095ceae7c717e8fc1c2c0e` (TIMING service) sai số tài chính (admin UI ĐÚNG 25.87M/2.07M/27.94M nhưng DOCX gen 152M/12.16M/27.94M + BBNT footer 155M/12.4M/167.5M + tạm ứng 82M + còn lại 85.4M ~5.87-5.99x admin UI). Root cause Hypothesis 2 confirmed: F-024 templates copy SAMPLE race DOCX với hardcoded numbers, converted SOME thành placeholders nhưng QUÊN 19 numeric strings trong Phụ lục/BBNT footer/3.2/tạm ứng/còn lại. Fix scope: 6 templates (timing/racekit/operations × contract + acceptance) + service flatten + audit/regen scripts. 25 files (1 service modify + 6 template binaries + 6 backup + 5 NEW backend lib/scripts/spec + 7 workflow). XML manipulation approach (Python pizzip extract + regex replace + repack) preserved formatting bit-perfect — alternative to LibreOffice edit. Manager Adjustment #1: existing `.backup/` BACKUP_DIRNAME pattern. Adjustment #2: extractDocxText test helper via pizzip. Coder safeguard beyond PRD: PAID contract WARN + special audit event `actor=f042-regenerate-script` for Finance team filter. 15/15 BR-42-* satisfied. 19/19 tests PASS (12 Coder TC-42 + 7 QC adversarial QC-TC-42-A01..A07 including deterministic 10x + concurrent Promise.all). 159/159 F-024 contracts regression PASS. 0 hardcoded financial numbers remaining (verified F-024 audit + independent grep + extractDocxText assertions). 0 CRITICAL/HIGH security un-mitigated. QC 6 phases ✅ APPROVED. Manager Code Review (MANDATE 2026-05-17) post-cherry-pick: flatten block line 1265-1300 clean conditional spread, 11 keys, vndAmountInWords reused for actualTotalWithVatInWords; 6 templates re-extracted + grep confirmed 0 hardcoded; backup files preserved. Acceptance-racekit edge case: 36.180.000 appears 4x with mixed semantic → ordinal-based replacement (2× totalAmount + 2× actualTotalWithVat). 8 TD-F042-* tracked (1 HIGH business — TD-F042-COMM-STRATEGY-PHASE2 Finance team chốt re-send strategy trong 1 tuần legal exposure window + 2 MED + 5 LOW). 3 NEW patterns minted: (1) DOCX template fix via XML manipulation (alternative to LibreOffice for bit-perfect preservation); (2) Context flatten cho docxtemplater (conditional spread pattern); (3) Audit + Regenerate script duo cho data-fix features. **PROD post-deploy MANDATORY:** multi-viewer DOCX verify (MS Word + LibreOffice + Google Docs per F-037 lesson) + audit script PROD run identify blast radius + regenerate batch (dry-run → --limit=10 → full) per Danny + Finance sign-off. Cherry-pick ab21227 → release/v1.8.7 `30b0ba1` + Manager deploy commit. Push origin release/v1.8.7 → CI deploy-production.yml auto-deploy PROD `admin.5bib.com`. Fast-forward push origin main → CI build-and-deploy.yml DEV. ZERO DB schema migration → instant rollback safe via revert + restore `.backup/`.)_

## 📝 In-flight features

| ID | Title | Status | Owner | Last update |
|----|-------|--------|-------|-------------|
| F-039 | Analytics Per-Event & Per-Day Enhancement | 🟡 INITIATED (defer pending) | — | 2026-05-15 |

> _(2026-05-18 FEATURE-042 🟡 INITIATED — Contract DOCX + BBNT Financial Number Bug Fix. BUGFIX HIGH SEVERITY. Danny phát hiện PROD bug 2026-05-18 khi xem DOCX hợp đồng `6a095ceae7c717e8fc1c2c0e` (TIMING service): admin UI hiển thị tài chính ĐÚNG (subtotal 25.870.000 + VAT 2.069.600 + tổng 27.939.600 + tạm ứng 13.969.800) nhưng DOCX gen ra SAI hết — Phụ lục 01 "Tổng tiền 152.000.000 / VAT 12.160.000 / Tổng sau VAT 27.939.600" (Tổng sau VAT đúng, 2 dòng trên SAI + math không add up); BBNT footer "Tổng tiền 155.101.000 / VAT 12.408.080 / Tổng sau VAT 167.509.080" (toàn bộ SAI ~6x admin UI); BBNT section 3.2 "Giá trị nghiệm thu 167.509.080" SAI; BBNT "tạm ứng 82.080.000" + "còn lại 85.429.080" SAI. Numeric analysis: 2 multiplier patterns ~5.99x (BBNT footer + total) + ~5.87x (Phụ lục Tổng tiền + tạm ứng) → 2 different bug paths OR shared mul với rounding edge case. Legal/finance risk: contracts đã gửi client với số SAI có thể dispute legally + vi phạm Luật quản lý thuế 38/2019. Affected scope unknown — cần audit ALL TIMING/RACEKIT/OPERATIONS contracts đã ship. Manager spot-check: `contracts.service.ts:243-256 calcTotals()` logic ĐÚNG (subtotal sum + vat round + total = sum), `upsertAcceptanceReport():1064-1123` logic ĐÚNG (derived from actualValues), `buildRenderContext():1210-1290` passes contract.subtotal/vat/total/lineItems correctly. Likely bug: TEMPLATE binary file `acceptance-timing.docx` + `contract-timing.docx` có placeholder mismatch HOẶC stale hardcoded values HOẶC data input bug acceptanceReport.actualValues unit_price sai. 7 PAUSE-42-* questions for BA: root cause classification (DB dump first), blast radius audit, fix scope 3 contract types, communication strategy re-send DOCX merchants, test fixture 3 contract types, regenerate batch script, lifecycle considerations FINALIZED. Pending Danny chạy `/5bib-prd FEATURE-042-contract-docx-financial-bug-fix`. Pattern reuse: F-035 cost field drop precedent (hand-pick mapping audit), F-037 DOCX colspan tblLayout precedent (binary template extract methodology).)_

> _(2026-05-18 FEATURE-041 ✅ DEPLOYED 2026-05-18 release/v1.8.6 commit 74960c5 — GA4 Analytics + Vietnam PDPA Consent. PROD live + GA4 admin 25 Custom Dimensions registered (Danny confirmed 2026-05-18). **MANAGER FLAG:** Skip DEV smoke (Danny accept risk) — rollback safe zero backend impact. Next: Marketing review GA4 Reports week 1 + DebugView post-deploy smoke + Lighthouse audit BR-41-13 verify.)_

> _(2026-05-18 FEATURE-041 ✅ DEPLOYED — GA4 Analytics + User Journey Tracking cho result.5bib.com. EXTEND_EXISTING. Danny request "cài đặt GA4 với chuẩn các step của người dùng để xem có thể cải tiến UX". 16 files (5 NEW lib/analytics/* + 1 NEW CookieConsentBanner + 1 NEW .env.production + 9 modify per Scope Lock + 1 QC e2e spec). ZERO backend impact. Vietnam PDPA Decree 13/2023/NĐ-CP compliant: Consent Mode v2 default `denied` + IP anonymization + 365-day localStorage versioned schema `{accepted, timestamp, version: 1}`. PII protection 2-layer: TypeScript compile reject `[K in PIIParamKey]?: never` + runtime sanitizer 12-key blacklist (incl. aliases name/fullName/athleteName). 17/24 events wired + 6 via PageViewTracker = 23/24 events (96%). All 4 conversion goals wired (share_athlete, share_result_image, download_certificate, generate_result_image — Marketing KPIs ⭐). 21 BR-41-01..21: 18 fully ✅ + 1 deviated-approved (BR-41-01 Manager Adjustment #1 mount trong `(main)/layout.tsx` NOT root → avoid conflict 4 solution layouts có gtag/GTM riêng: G-ND6VCY2B57 + GTM-WNJV5PD9 + GTM-PLR9LHLZ + GTM-TGL3KFCS) + 2 deferred (BR-41-13 LCP measure post-deploy, BR-41-17 opt-out UI Phase 2). Manager Adjustment #2: extended google-analytics.tsx Consent Mode v2 default block với `wait_for_update: 1500` matches CookieConsentBanner SHOW_DELAY_MS. TypeScript strict clean `pnpm tsc --noEmit` exit 0 toàn frontend. Side artifact: GA4-ADMIN-CONFIG-GUIDE.md (26KB operations runbook 10 bước cho Danny parallel config GA4 admin UI: 24 dimensions register + 4 conversions mark + DebugView access + funnel exploration template). QC 6 phases passed: Phase 1 regression zero backend impact + Critical Adjustments #1+#2 verified, Phase 2 security 12 vectors 0 CRITICAL/HIGH un-mitigated, Phase 3 wrote ga4-consent-flow.spec.ts 17 Playwright cases (15 functional + 2 stability 10x), Phase 4 deferred execution (no `@playwright/test` installed — pre-existing convention same as chip-verify-kiosk.spec.ts), Phase 5 PRD compliance 86% (18/21 BR fully ✅ + 1 deviated-approved + 2 deferred), Phase 6 5 persona walkthrough với numbered steps. QC verdict ⚠️ APPROVED WITH CAVEATS → Manager ACCEPT all 6 caveats (4 partial wiring TD + no test runner Danny-PAUSE-approved + LCP defer + search_term PII MED + privacy-policy 404 UX gap + Playwright not installed). 14 TD-F041-* tracked (1 MED TD-F041-SEARCH-PII + 1 MED TD-F041-NO-TEST-RUNNER + 12 LOW/NIT all non-blocking). **NEW patterns minted (4):** (1) "Analytics event taxonomy SSOT" — single const EVENTS + TypeScript `[K in PIIParamKey]?: never` compile-time reject + runtime sanitizer blacklist; (2) "Consent Mode v2 + localStorage versioned schema" — Vietnam PDPA Decree 13/2023/NĐ-CP compliance pattern; (3) "SPA pageview hybrid Next.js App Router" — Enhanced Measurement auto page_view + custom PageViewTracker route regex emit CONTEXT events với entity params; (4) "Mount scope per route group (NOT root)" — FE feature subset routes mount trong route-group layout để avoid sub-layout conflict. Coder skipped unit tests per Danny PAUSE Option 1 (frontend KHÔNG có Vitest/Jest setup pre-F-041 — existing province-normalize.spec.ts also docs-only) → coverage strategy: strict TS types + runtime PII sanitizer + 17 Playwright E2E specs ready-to-run + manual smoke. Self-Review 10/10 complete. Manager Code Review (skill MANDATE 2026-05-17) — spot-checked 6 critical files (events.ts, consent-manager.ts, useGAEvent.ts, page-view-tracker.tsx, CookieConsentBanner.tsx, google-analytics.tsx) clean, both Adjustments #1+#2 correctly implemented. **NOT YET pushed to remote** — Danny next: parallel GA4 admin config Bước 1-6 (~30 phút) + git add 15 F-041 paths (exclude pre-existing F-036 admin-seo + tsconfig uncommitted) + push để CI auto-build → DEV deploy `result-fe-dev.5bib.com` smoke verify → release/v1.8.6 cherry-pick PROD `result.5bib.com`. Side: `GA4-ADMIN-CONFIG-GUIDE.md` BƯỚC 10 post-deploy 10-step smoke verification per persona walkthrough.)_

> _(2026-05-17 FEATURE-040 ✅ DEPLOYED via release/v1.8.5 commit f50c003 — Finance Real Fee Compute. BUGFIX + EXTEND_EXISTING. Danny challenge "Doanh thu hiện 18,284,000 GMV cho contract 6a095d81... sai bản chất" → replace gross GMV với fee 5BIB thật (target ~1,279,880 = 7% × GMV). 4-tier source priority cascade: RECONCILIATION (BBNT signed status whitelist signed/reviewed/completed/sent) → MIXED (recon + self-compute period gap) → SELF_COMPUTE (MerchantConfig rate cascade 3-tier: merchant_configs.service_fee_rate → contract.revenueShare.feePercentage → 5.5% default + Logger.warn) → ESTIMATED (contract.totalAmount fallback). 22 files (Backend 13 + Admin 9). Backend NEW: getFeeForContract() + getFeeForContractsBulk() + computeSelfFee() (Option A 1 query CASE statement với 6 metrics) + computePeriodCoverage() + getReconciledFeeForContract() (TD-F016 legacy warning rate-limited + TD-F003-02 dup SUM). 4 NEW DTO: FeeBreakdownDto + ReconciledFeeSliceDto + SelfComputeSliceDto + FeeSourceMixDto. NEW endpoint `GET /api/finance/contracts/:id/fee-breakdown` (admin-only). NEW fee-breakdown.controller.ts split (D4). Cache TTL 60s → 3600s, key embed tenantId (D2). 7 invalidation hook sites. Admin 3 NEW components: FeeSourceBadge + SourceMixStrip + FeeBreakdownPanel. 18 BR-40-01..18. 19 TC-FE + 32 cũ regression = 205/205 finance+recon, 1159 full backend regression. Admin tsc 0 errors F-040. MySQL EXPLAIN PLAN race 194 ✅ verified eq_ref PRIMARY KEY (best possible), no degradation. Decision D3 INLINE duplicate fee formula → preserve 102 reconciliation tests baseline + TD-F016 stability. QC ✅ APPROVED 6 phases. Manager Code Review (skill update 2026-05-17 MANDATORY per Danny "mày review code chưa?") — 5 critical files spot-checked với findings green points + 3 minor concerns + 0 red flags. 7 cache hook grep verified vs Coder claim. 9 TD-F040-* tracked (TD-F040-EXPLAIN-PLAN ✅ RESOLVED post-deploy). **NEW patterns minted:** (1) "Source attribution + companion cache keys" — multi-source feature uses enum `feeSource` + parallel cache keys; (2) "Inline duplicate vs extract trade-off" — when 2 modules share business formula + risk drift cross-domain, inline ACCEPTABLE để preserve regression safety. PROD verified contract 6a095d81e7c717e8fc1c2da1 UI hiện 1.28M fee thật. VPS down event 18:30-22:00 ICT KHÔNG liên quan F-040 — infrastructure-level, containers stable Up 4h post-recovery. SIDE: skill 5bib-manager updated 2026-05-17 với MANDATORY independent code review trước close feature + 5-item anti-pattern table reject rubber-stamp.)_

> _(2026-05-16 FEATURE-038 ✅ DEPLOYED through full 5-gate workflow (Init→PRD→Plan→Code→QC→Deploy) — Finance Contracts List with P&L Per Row. Branch in worktree `funny-kirch-90e777` (release/v1.8.1 base) — NOT YET pushed/deployed to PROD or DEV (Coder verified backend `nest build` + admin `next build` PASS locally, awaits Danny push decision per anti-cowboy workflow rule). Backend 8 file (3 NEW DTO/controller + 5 modify: pnl.service.ts adds 4 private helpers + getContractsList(), pnl.service.spec.ts +14 tests TC-CL-01..14, finance.module.ts register, cost-items.service + contracts.service extend flush BOTH patterns `pnl:dashboard:*` + `pnl:contracts-list:*`). Admin 7 file (5 NEW components: ContractsListClient + Table + FooterSummary + MarginLegendBanner + EmptyState; 2 modify: finance-api.ts + page.tsx REWRITE from F-028 Phase 1 placeholder). 1 regression test assertion fix `cost-items.concurrency.spec UP-08 scanStreamCalls 2→4` (necessary consequence of BR-38-09 dual-pattern flush — documented in 03-coder-implementation.md, NOT scope creep). Endpoint NEW `GET /api/finance/pnl/contracts?period=&dateFrom=&dateTo=&page=&limit=&sortBy=&sortDir=&q=` (200/400/401/403). Cache `pnl:contracts-list:<sha256-16char>` TTL 60s. 11 BR-38-01..11: status whitelist ACTIVE+COMPLETED (BR-PNL-08 inherited), period filter 6 presets (no `all_time` — uses `last_12_months` widest), P&L additive F-036, sort 5 cols default anchorMonth DESC, search combined 3 fields + escapeRegex ReDoS defense, pagination 20/50/100 max 100, margin tier 4-class, cache 60s + invalidation, admin-only LogtoAdminGuard + page isAdmin gate defense-in-depth, URL deep-link state. 46/46 pnl.service.spec PASS (32 cũ regression OK + 14 mới). 250/250 broader finance+contracts module regression PASS. QC ✅ APPROVED 0 CRITICAL/HIGH security threats (13/13 vectors mitigated). 8 TD-F038-* tracked: SDK-REGEN, REFACTOR-EXTRACT (~80 LoC duplicate computeContractRows ↔ getDashboardData body — deliberate copy to guarantee 32 cũ regression), MONGO-SORT (in-memory acceptable <1K contracts), EXPORT-LIST (PAUSE-38-06 defer Phase 2), FILTERED-COST-CATEGORY (filteredTotals.costByCategory dataset-wide — unused on list page now), PAGE-CLAMP (?page=99 not clamped), AUTH-INTEGRATION-TEST (HTTP 401/403 via E2E walkthrough, not unit), PERF-SLA-MEASURE (p95 < 500ms cold / <100ms warm cần đo empirical Manager+BA walkthrough phase). **NEW patterns minted:** (1) "Dual-pattern cache flush" — single helper iterates `['pnl:dashboard:*', 'pnl:contracts-list:*']` để invalidate atomic both; (2) "URL deep-link sync 2-level debounce" — local input state 0ms + applied state 400ms + URL push debounced; (3) "Defense-in-depth admin gate" — backend Guard + page-level `isAdmin` check BEFORE client mount để zero wasted fetch. **NOT YET pushed to remote** — Danny next step: empirical Manager+BA UI walkthrough localhost docker-compose restart admin với code mới, verify performance SLA + persona journeys, THEN push để CI auto-deploy DEV → cherry-pick release/v* PROD.)_

> _(2026-05-15 FEATURE-038 🟡 INITIATED — Finance Contracts List with P&L Per Row. BUGFIX + EXTEND_EXISTING. Danny audit phát hiện `/finance/contracts` là placeholder page từ F-028 Phase 1 era (2026-05-12) trong khi `/finance` đã có aggregated dashboard. Inconsistency: nav "P&L theo HĐ" trỏ tới placeholder thay vì dashboard. Decision Danny 2026-05-15: thay placeholder bằng **list HĐ với KPI inline per row** (contractNumber / partner / revenue / cost / profit / margin tier) + click row → `/finance/contracts/[id]` detail. Scope: backend NEW endpoint `GET /api/finance/pnl/contracts` paginated (reuse `getDashboardData()` compute path no-slice) + DTO + admin rewrite `/finance/contracts/page.tsx` từ placeholder thành list table với TanStack Query. Risk 🟢 LOW backend (reuse proven F-028 + F-036 logic) + 🟢 LOW admin (mirror `/contracts` pattern) + 🟡 MED perf (full list cần Redis 60s cache). 6 PAUSE-38-* questions for BA: status whitelist DRAFT include?, default sort, search fields, page size, margin legend, export defer. Pending Danny chốt 6 PAUSE hoặc accept defaults (giữ BR-PNL-08 ACTIVE+COMPLETED + sort anchorMonth DESC + search 3 fields combined + page 20 + margin legend show + export defer Phase 2).)_

> _(2026-05-15 FEATURE-037 ✅ DEPLOYED via `release/v1.8.1` — DOCX colspan widths + tblLayout fixed + tableHeader + cantSplit + bỏ logo (5 commits 98fc18f→0d93d4b). Reconciliation DOCX export bug fix progression v1→v2→v3A→v3B→v4 trong cùng branch fix/F-037-docx-colspan-width. Danny screenshot phát hiện qua Google Drive viewer / Mac Preview: text wrap vertical mỗi ký tự + logo distort. Root cause 3-layer: tcW missing per colspan cell (v1) + tblLayout autofit default ignored tcW (v2) + tblGrid auto-gen default 100 DXA per col when columnWidths missing (v3A) + page break ngắt giữa table không có header repeat (v3B) + logo image cell distort qua strict renderers (v4 = bỏ). 16/16 tests PASS (12 existing + 4 NEW TC-DOCX-COL-04..07). Lesson minted: test multi-viewer (KHÔNG chỉ MS Word smart-fit). KHÔNG có 05-manager-deploy.md formal — workflow gap, sẽ sync sau.)_

> _(2026-05-14 FEATURE-036 ✅ DEPLOYED via branch fix/F-035 — P&L additive semantic fix. Bundled với F-035 ship cùng. Danny screenshot HD CTTXT5-5BIB-20: chi phí 1M cost_item override 185M estimated → margin 99.5% sai. Root cause: F-033 design priority chain "actual REPLACE estimated" sai semantic. Fix: ADDITIVE — totalCost = estimatedCost + actualCost. Backend pnl.service detail + dashboard path. 5 NEW TC-LIC-* (additive) + 1 NEW TC-LIC-06 reproduce Danny scenario (185M+1M=186M margin 11.1%). Admin pnl-summary-card breakdown UI "Ước tính" + "Phát sinh" + 3 badges KẾT HỢP/ƯỚC TÍNH/THỰC TẾ. 32/32 pnl.service.spec PASS. Note: F-036 folder name claimed bởi "seo-subdirectory-giai-chay" feature khác — F-036 P&L additive shipped trong branch fix/F-035-edit-dialog-line-items-width commit a8ad737.)_

> _(2026-05-14 FEATURE-035 ✅ DEPLOYED via branch fix/F-035 → main + release/v1.8.1 (5 commits: 9f4cb64 + 797fa85 + 7bc1050 + a8ad737 + eb452e5). Edit dialog cost field persist + P&L additive semantic. Triple-bug chain: dialog narrow shadcn `sm:max-w-sm` 768px (v1 9f4cb64 width + table-fixed + truncate + sticky header/footer) → cost field drop silent triple-quên (v2 797fa85 backend create+update mapping + 7bc1050 admin buildInitialState) → P&L semantic actual REPLACE vs ADDITIVE (v3 a8ad737 F-036 fix). Workflow proper: branch off main → push branch only (KHÔNG deploy PROD) → Danny localhost UAT → merge main + release/v1.8.1. FIRST CORRECT WORKFLOW trong session post-cowboy-ops 11-commits-thẳng-release. Memory hardening commit eb452e5: 4 anti-patterns + workflow rule "PROD deploy phải qua DEV UAT, KHÔNG push thẳng release/v* trừ critical incident".)_

> _(2026-05-14 FEATURE-034 ✅ DEPLOYED via release/v1.8.1 commit d491ada — Force-edit any status. Danny request "tao muốn HĐ có thể sửa được trong mọi trường hợp, vì cơ bản thật ra cũng lắm chuyện phết". Backend remove DRAFT-only + TERMINAL_STATES block trong contracts.service.update(). Audit emit `contract.update.force` với previousStatus + editedFields metadata. P&L cache flush post force-edit. KEEP status manipulation block (status changes via dedicated endpoints). Admin detail page enable "Chỉnh sửa" button non-DRAFT + confirm dialog status-aware warning (ACTIVE/COMPLETED/CANCELLED/REJECTED 4 messages khác nhau). Edit dialog banner amber cảnh báo. Save success toast 7s nhắc regenerate DOCX. 5 NEW TC-F034-01..05 tests. Risk acknowledged Danny: legal mismatch DOCX physical vs DB → admin tự responsibility; acceptance/payment không auto-recompute → admin check manual.)_

> _(2026-05-14 FEATURE-033 ✅ DEPLOYED — Promo Hub race_calendar sync from MySQL platform on-sale phase. EXTEND_EXISTING F-027. Workflow FULL 5-gate (BA→Plan→Code→QC→Deploy) — NO hotfix shortcut. Backend: NEW entity `RaceReadonly` (12/70+ cols `races` MySQL `5bib_platform_live` named connection `'platform'`) + NEW DTO `RaceOnSaleResponseDto` + NEW endpoint `GET /api/promo-hubs/races-on-sale` (public, no auth) + service `findRacesOnSale()` filter status=GENERATED_CODE+is_delete=0+is_show=1, sort default registration_start_time ASC, Redis cache `promo-hub:races-on-sale:<limit>:<sort>` TTL 60s, graceful degrade Redis/MySQL fail → return []. Frontend `RaceCalendarSection` rewrite branch by `source` config: `platform_on_sale` (F-033 default) fetch new endpoint render `PlatformRaceCalendar` w/ countdown badge nếu <30 days; `result_active` (F-027 legacy, default for missing source → backward-compat BR-PH33-12) fetch old `/api/races`. Admin form `SectionConfigDialog` adds Source select + conditional Sort (platform) vs Status (result). Helper `getTicketUrl()` `5ticket.vn/event/<urlName>` in `internal-urls.ts`. 38/38 unit tests PASS (28 baseline + 10 new). 2 post-deploy hotfixes within F-033 workflow: (1) commit `7fd6d80` drop `url_name IS NOT NULL` filter — PROD shows 19/19 active races có NULL url_name → fallback DTO `slug = urlName?.trim() || raceId` (5Ticket convention dùng race_id when no slug); (2) commit `35877a7` register `RaceReadonly` in `app.module.ts` 'platform' DataSource entities array — `forFeature` alone insufficient, MUST also register at `forRoot` else `No metadata for "RaceReadonly" was found` runtime. **NEW pattern minted:** TypeORM non-default connection requires entity registration at BOTH `forRoot.entities[]` (DataSource) AND `forFeature(...)` (Repository DI). 6 TD-F033-* tracked: TD-F033-06 HIGH = verify 5Ticket route `event/<numeric_id>` actually serves (else need backfill `url_name` for 27 races OR 5Ticket add alias route). PROD smoke test: 3/3 races returned correctly (raceId 104 MuCangChai + 156 Aviwin + 172 Cuc Phuong), TTFB cold ~270ms cached <100ms. Live: https://result-api.5bib.com/api/promo-hubs/races-on-sale)_

> _(2026-05-13 FEATURE-032 🟡 INITIATED — Partner Import Excel. EXTEND_EXISTING F-024 Partners domain. Danny ngay sau F-031 ship: "thêm cả tính năng import đối tác nữa, tao quên mất". Pattern reuse 100% F-031 — swap schema (Partner 11 fields entityName/shortName/taxId/address/representative/position/bankAccount/bankName/phone/email/notes) + admin route (`/contracts/partners`). Đề xuất 11 cols VN headers. Duplicate key recommend taxId primary + entityName fallback (MST stable hơn tên). 7 PAUSE-32-* — 6 reuse defaults từ F-031 (2-step Preview→Confirm, Max 200, createdBy=userId, Empty skip+report, CÓ template, Excel format), 1 mới (PAUSE-32-02 dedup key). Backend 6 file (3 NEW + 3 modify) + Admin 3 file (1 NEW + 2 modify). ETA ~2h pattern reuse. Đợi Danny chốt "A" theo F-031 defaults hoặc trả lời cụ thể PAUSE-32-02. Next /5bib-init bumps to 033.)_

> _(2026-05-13 FEATURE-031 ✅ DEPLOYED via `release/v1.7.9` — Service Catalog Excel Import. Commit `6e30ef9` on main → push `origin/main` + `origin/release/v1.7.9` → CI auto-deploy PROD. 9 file (3 BE NEW + 3 BE modify + 1 admin NEW + 2 admin modify): NEW DTO 4 shapes + service `parseExcel + validateRow + bulkInsert + generateTemplate` + spec 9 TC + controller 3 routes literal BEFORE :id + module provider + `findByNameCategoryPairs` method + admin Dialog 2-step state machine + lib helpers + table button. 9/9 NEW TC-IM-* PASS + 16/16 service-catalog regression. QC ✅ APPROVED 0 CRITICAL/HIGH security. 7/7 PAUSE-31-* + 6/6 Plan AC compliance. Category parser accept VN labels (`Tính giờ`/`Racekit`/`Vận hành`/`Chung`) + English enum case-insensitive. Reuse: ExcelJS (reconciliation), FileInterceptor (F-024 contract-templates), jsonFetch (contracts-api), LogtoStaffGuard (F-029). 5 TD-F031-* all LOW/MED: SDK-REGEN defer post-deploy, RAW-FETCH consistent pattern, FE-UNIT-TEST block TESTSTACK, MULTER-MIME future magic-bytes, OR-QUERY future scaling beyond 200. **NEW pattern minted:** "2-step Excel Import UX" (Preview→Confirm với badges Hợp lệ/Trùng/Lỗi) — reusable cho future bulk import (athletes, sponsors, partners). Rebase trên top F-027 Promo Hub merge — clean fast-forward push.)_

> _(2026-05-13 FEATURE-031 🟡 INITIATED — Service Catalog Import Excel. EXTEND_EXISTING F-024 Contract Management Module. Danny report admin trang "Hợp đồng → Danh mục dịch vụ" (`/contracts/services`) chỉ có "+ Thêm dịch vụ" 1 item/lần, cần import nhiều dịch vụ từ Excel để setup ban đầu nhanh. Schema F-024 hiện tại: name + category (TIMING/RACEKIT/OPERATIONS/GENERAL) + unit + referencePrice + referenceCost + description + sortOrder. Đề xuất scope: NEW `POST /api/contracts/service-catalog/import-excel` + `GET /import-template` + service `service-catalog-import.service.ts` + admin dialog 2-step Preview→Confirm. 7 PAUSE-31-* pending Danny: Excel format columns, duplicate behavior (recommend Skip + report), dry-run 2-step (recommend yes), max rows 200, audit createdBy=userId, empty value skip+report, download template button. ETA ~2.5h (skip BA pattern proven F-025+F-030). Library reuse: ExcelJS đã có sẵn từ reconciliation xlsx.service.ts. Next /5bib-init bumps to 032.)_

> _(2026-05-13 FEATURE-030 ✅ DEPLOYED via `release/v1.7.8` — Reconciliation Add-on Visual + 5BIB Provider Config. Commit `f980228` on main → push `origin/main` + `origin/release/v1.7.8` → CI auto-deploy PROD. 8 file (5 Scope Lock + 3 test artifacts justified): `src/config/index.ts` extend (Joi 8 PROVIDER_* + env.provider namespace fail-soft `.default()`), `calc.service.ts` fix add_on_price dedup bug (order-level field cộng dupe per-row → DEDUP by order_id Set, áp dụng cho cả ORDINARY + CHANGE_COURSE branch), `xlsx.service.ts` render col 6 add_on per-line + col 4/5 '—' indicator + bottom Tổng include, `docx.service.ts` import env replace 6 hardcoded refs + signature uppercase + conditional bottom row "Vật phẩm bổ sung", 3 NEW spec (10 TC-AO-*: 5 calc + 1 xlsx + 4 docx). 10/10 NEW PASS. 8/9 reconciliation domain suites PASS (controller.spec failure pre-existing F-029 regression — flagged TD-F029-INHERITED-CTRL-SPEC, NOT F-030). QC ✅ APPROVED 0 CRITICAL/HIGH security. 5/5 PAUSE-30-* compliance + 6/6 Plan AC. Math Zaha fixture: Section 1 18,422,200 = Section 3 (18,123,200 line items + 299,000 add-on). 5 TD-F030-* flagged all LOW. **NEW pattern minted:** "Fail-soft env defaults" (Joi `.default()` cho business legal info thay vì `.required()` — KHÔNG outage container restart, deploy resilient). 5BIB info correct: "Tầng 9 Hồ Gươm Plaza 102 Trần Phú Hà Đông" replace legacy "Tôn Thất Thuyết" (Danny: "đừng tự quyết định" — lesson learned).)_

> _(2026-05-13 FEATURE-030 🟡 INITIATED — Reconciliation Add-on Visual Display + 5BIB Provider Info Config. BUGFIX + EXTEND_EXISTING phát hiện qua Danny PROD report 2026-05-13 trên đối soát Zaha Hai Phong Legacy Marathon 2026 tháng 4: (1) XLSX Section 1 gross = 18,422,200 vs Section 3 line items breakdown total = 18,123,200, vênh 299K = giá trị áo (add-on) bị orphan trong line items render — col 4/5/6 hardcode 0 thay vì đọc `li.add_on_price` đã được aggregate đúng trong `calc.service.ts:106`. (2) 5BIB provider info hardcode SAI trong `docx.service.ts:533-543` từ legacy commit `205a1c1` pre-workflow — địa chỉ cũ "Tôn Thất Thuyết" thay vì trụ sở thực "Hồ Gươm Plaza Hà Đông". 5 PAUSE-30-* ALL RESOLVED: Danny chốt Option 2 (visual fix only, KHÔNG đổi gross — add-on vẫn tính phí 5.5%), KHÔNG migrate recon cũ (F-025 bulk delete xóa thủ công), config = env vars `PROVIDER_*` (option a), 7 fields đầy đủ (CTCP 5BIB, Hồ Gươm Plaza 102 Trần Phú Hà Đông, MST 0110398986, SĐT 0373398986, Nguyễn Bình Minh GĐ, TK 110398986 MB Bank). Scope: 4 file backend modify + 1 NEW config + .env.example. Skip BA gate, /5bib-plan trực tiếp. Estimated ~2h. Manager initially mis-numbered as F-028 nhưng F-028 (deal-pnl-tracking) + F-029 (hardening) đã used → renamed to F-030. Next /5bib-init bumps to 031.)_

> _(2026-05-13 FEATURE-029 ✅ DEPLOYED — Hardening Phase 1 + Phase 1.1 extension. Branch `5bib_hardening_phase_1_v1` HEAD off main `01c2950`. Phase 1: HIGH-RR-01 race-result draft filter (1 endpoint) + HIGH-PERF-01 P&L N+1 → batch + HIGH-RBAC-01 wrap 37 admin pages + display sweep 9 sites + permissions.helper.ts new (logto-auth). Phase 1.1 extension (post-QC v1 MISS-01 finding): extract enforceRaceVisibility/resolveRaceIdFromCourseId helpers + apply to 13 sibling race-result endpoints (leaderboard/athlete/certificate/compare/filters/stats×3/country-rank/percentile/result-image/badges/share-count). 38/38 F-029 unit tests PASS (24 race-result + 6 pnl + 8 fee). 0 new typecheck errors. QC v2 ✅ APPROVED no PROD BLOCKER — TD-F029-NEW-01 RESOLVED. 2 new TDs flagged for FEATURE-030 (NEW-02 POST result-image + NEW-03 global search). 5 CRIT defer track-only (Danny chốt "dev có lý do"). Workflow exception: PRD BR-HD-01 implicit scope extended via Manager Plan Phase 1.1 extension (BA artifact untouched — pure defense-in-depth completion). Coder Phase 1 + Phase 1.1 fully documented `03-coder-implementation.md` + `03b-coder-phase-bc-log.md`. NEW pattern minted: `enforceRaceVisibility(raceId, user?)` public helper extracted in service — reusable from controller for endpoints that bypass standard public methods.)_

> _(2026-05-12 ULTRAREVIEW FULL PROJECT — main `01c2950` reviewed. 111 findings (4 CRIT / 22 HIGH / 47 MED / 38 LOW). Verdict 🟡 APPROVED WITH CAVEATS — DEV OK, PROD `release/v*` next BLOCKED until 5 CRIT fixed. **Danny defer all 5 CRIT 2026-05-12 — "dev làm là có lý do"** (CRIT-01 `dump.rdb` test file, CRIT-02 JWT fallback dev local, CRIT-03 upload validation có WAF/CloudFront cover, CRIT-04 SSRF vendor RaceResult IP whitelist FW level, CRIT-CI-01 deploy workflow stable). Track trong `known-issues.md` "Defer 2026-05-12". Memory sync batch 1 applied: 11 missing shipped rows F-008/F-016/F-017/F-018/F-020/F-021/F-022/F-023/F-024/F-026/F-028. Bump module count 24→33 in codebase-map. Architecture.md add F-024/F-027/F-028 service decomp. SDK regen pending. Next batch = `/5bib-init FEATURE-029-hardening-phase-1` covers HIGH-RR-01 draft filter + HIGH-RBAC-01 page gates + HIGH-PERF-01 N+1 batch + display sweep ~8h coder work.)_

> _(2026-05-12 FEATURE-028 ✅ DEPLOYED on main `01c2950` (ad-hoc merge, NO 05-manager-deploy.md) — Deal P&L Tracking Module 3 phases NEW_MODULE. Phase 1 backend P&L compute (`pnl.service.ts`, `cost-items.service.ts`, `fee.service.ts` cross-DB MySQL F-016 pattern) + ServiceCatalog `referenceCost` + `LineItem.catalogItemId` MongoId ref. Phase 2 dashboard aggregated (`pnl-dashboard.service.ts` BR-PNL-08 strict `status: $in:['ACTIVE','COMPLETED']` whitelist + period filter 5 preset + custom range). Phase 3 cost suggestions (catalog × quantity) + margin badge 3-tier 🟢🟡🔴. Critical bugs gặp: MySQL `o.tenant_id` not exists fix → JOIN via order_line_item; backend cũ DTO sau rebuild fix kill PID 98258+19555; infinite render loop useSetCrumb 2694 errors fix useRef pattern; raw enum `last_3_months` render fix force `{PERIOD_LABEL[period]}`; title slice(-6) ObjectId fix contractNumber fallback. Display Convention rule ban hành commit `f18da46` KHÔNG render raw enum/snake_case. ULTRAREVIEW phát hiện HIGH-PERF-01 N+1 MySQL trong `pnl.service:336-340` → defer track FEATURE-029. **Manager note:** 11 shipped rows missing được append batch 1 today, F-028 close ad-hoc via memory sync chứ KHÔNG qua `/5bib-deploy` workflow vì Danny merge main trước ultrareview.)_

> _(2026-05-11 FEATURE-025 ✅ DEPLOYED via `release/v1.7.7` — Reconciliation Bulk Delete. Commit `84155aa` on main → push `origin/main` + `origin/release/v1.7.7` → CI `deploy-production.yml` auto-deploy PROD. 6 file (5 Scope Lock + 1 QC artifact dto.spec). Backend: `POST /reconciliations/delete-batch` Mongoose `deleteMany({_id:{$in:ids}})` + Logger.warn audit + DTO 1-50 IsMongoId. Admin: button "Xóa hàng loạt (N)" + Dialog 2-button ZERO friction (Danny: "không bắt tao làm nhiều") + idempotent UX. 24/24 NEW tests PASS (5 Coder TC-DM + 3 QC TC-QC-DM + 9 QC TC-DT + 7 regression). 82/82 reconciliation domain regression. 0 break F-003/F-004/F-016. QC ✅ APPROVED 0 CRITICAL/HIGH security threats. 5 PAUSE-25-* compliance: ZERO friction, KHÔNG status guard, Logger.warn đủ, S3 lifecycle 24h, max 50. 5 TD-F025-* flagged. New pattern minted: "Bulk delete idempotent return shape" (deleted + not_found counts thay vì throw 404) — added to conventions.md.)_

> _(2026-05-11 FEATURE-027 🟡 INITIATED — Promo Hub Configurable Landing Page. NEW_MODULE cho admin + frontend public. CMS-driven promotional landing page builder — kill strategy vs iRace AddMe.vn Linktree clone. Configurable sections (hero, race_calendar, promo_banner, cta_buttons, sponsors, stats, rich_text, featured_races, recent_results) + auto-sync from races/sponsors DB + click analytics. New MongoDB collection `promo_hubs` + Redis cache `promo-hub:<slug>` 60s + S3 `promo-hub/` prefix. Independent module — ZERO touch existing services. 8 PAUSE-PH-* pending Danny. Phase 1 estimate 2-3 weeks (5 section types + CRUD + public render). Next /5bib-init bumps to 028.)_

> _(2026-05-11 FEATURE-025 🟡 INITIATED — Reconciliation Bulk Delete. BUGFIX + EXTEND_EXISTING phát hiện qua Danny PROD report 2026-05-11 "chọn vài đối soát ấn xóa → lỗi 500 lần đầu, lần 2 chỉ xóa 1". Trace ra 2 layer: (1) ROOT CAUSE 500 = admin Next.js proxy crash `TypeError: Response constructor: Invalid response status code 204` khi backend trả 204 cho DELETE success (Web Fetch spec không cho body với null-body status 204/205/304). HOTFIX out-of-band commit `bab4c44` + `release/v1.7.5` push 2026-05-11 sweep 3 proxy (admin + frontend + crew). (2) FEATURE GAP thực sự = KHÔNG có bulk delete UI/endpoint, user click trash icon per-row chỉ xóa 1. F-025 = build proper bulk: backend `POST /reconciliations/delete-batch` (Mongoose `deleteMany({_id:{$in:ids}})`) + admin button "Xóa hàng loạt (N)" + confirm modal có warn cho `signed`/`sent` recon + per-item result. 5 PAUSE-25-* pending Danny. Pattern reuse F-014 BulkActionBar UI + F-016 `_id` flow. Next /5bib-init bumps to 026 (already used → 027).)_

> _(2026-05-11 RELEASE v1.7.3 ✅ PROD LIVE — F-023 Admin Dashboard + F-026 Admin Analytics deploy thành công. Branch flow: `main` `a7346d2` (4 BLOCKER fix) → `release/v1.7.3`. Coder commit `a7346d2` fix 4 TD: TD-F026-REPEAT-TREND-FORMULA (SQL subquery thật, KHÔNG còn `100 if total>0`), TD-F023-PERF-SLA (N+1 Redis → pipeline trong live-races + pending-tasks, từ 2N+N RTT → 2 RTT), TD-F023-SDK-REGEN (regen + xóa dashboard-sdk-shim.ts, 6 widget update import sang `api-generated/sdk.gen`), TD-F023-REDIS-SCAN (`redis.keys` → `redis.scan COUNT 100` trong system-status). 25/25 unit test PASS, build PASS, tsc clean. QC pass thứ 2 (golive-readiness) APPROVED sau fix. Danny verified PROD live 2026-05-11. 4 TD còn defer non-blocking: TD-F023-CLICK-NAVIGATE, TD-F023-LINK-STRATEGY, TD-F026-EXPORT-STUB (track Phase 2), TD-F026-CACHE-INVALIDATE (cron 1h tự refresh đỡ), TD-F023/F026-TANSTACK-QUERY (5 site raw fetch trong analytics/page.tsx — defer sprint sau, Redis cache đỡ tải). Phase 2 admin redesign batch 1 chính thức ĐÓNG.)_

> _(2026-05-10 FEATURE-024 🟡 INITIATED — Contract Management Module. NEW_MODULE cho admin. 4 contract types (ticket_sales, timing, racekit, operations) + acceptance report + payment request. 2 provider entities (5BIB MST 0110398986 / 5Solution MST 0111213998). Auto-fill từ race/merchant data. DOCX/PDF generation. Pattern reuse F-004 DOCX gen + F-014 admin list. 10 PAUSE conditions pending Danny. Strategic Scout confirmed massive competitive gap — no platform has contract management built-in. Next /5bib-init bumps to 025.)_

> _(Bumped 2026-05-10 — FEATURE-023 + FEATURE-026 ✅ DEPLOYED cùng push main (Phase 2 admin redesign batch 1). F-023 Admin Dashboard Redesign giai đoạn 2 màn 01 (commit `c0d799e` branch `5bib_admin_dashboard_v1`) — 24 file Scope Lock, dashboard module 7 services + audit module mới + 6 widgets + CreateRaceDialog extract. Kế thừa F-022 visual tokens. 50/50 unit test PASS. ⚠️ APPROVED WITH CAVEATS — 5 TD trước PROD: TD-F023-SDK-REGEN, TD-F023-CLICK-NAVIGATE, TD-F023-LINK-STRATEGY, TD-F023-PERF-SLA, TD-F023-REDIS-SCAN. F-026 Admin Analytics — Ops/Biz/Finance/CS version (commit `2e709f9` branch `5bib_admin_analytics_v1` + `f2d9a4d` rename "Phân tích vận hành" + move filter). 28 file Scope Lock + 9 spec tách. 51/51 test PASS (vượt target 39 → 130%). 5 RED LINE invariants verified (BR-01 paid only, BR-04 race draft exclude, BR-05 dedupe athletes_id NOT bib). Hybrid scope phương án C — platform-wide default + drill-down per-race. Period-resolver utility + hourly cron aggregator + 6 services (RepeatAthlete, MerchantChurn, TimeToFill, ClaimRate, RefundCancel, GeographicDemographic) + 8 widget. ⚠️ APPROVED WITH CAVEATS — 3 TD: TD-F026-EXPORT-STUB, TD-F026-REPEAT-TREND-FORMULA, TD-F026-CACHE-INVALIDATE. **Quan trọng:** Danny test localhost xác định F-026 KHÔNG phải MKT dashboard — 4/8 widget (Churn, Time-to-Fill, Claim, Refund) phục vụ Ops/Finance/CS. Heading rename "Phân tích vận hành". Filter row move xuống dưới heading vì chỉ áp khu này. MKT analytics tương lai → feature riêng (Funnel, CAC, Cohort, LTV, Email engagement, Geographic heatmap, Persona segmentation). New convention: period-resolver utility + analytics-invariants.spec.ts cover 5 RED LINE BR-01 → BR-05. Bài học mới: Multi-agent worktree rule — KHÔNG được spawn 2 Coder parallel cùng worktree (F-026 retry vì 1 lần work mất do worktree conflict). Branch flow: `5bib_admin_dashboard_v1` + `5bib_admin_analytics_v1` → merge `5bib_admin_phase2_test` → main (deploy DEV only, KHÔNG release/v* PROD lần này). F-024/F-025/F-027 còn reserved cho Phase 2 admin redesign tiếp — Manager đã từ chối init đồng loạt 3 feature theo yêu cầu Danny. Next /5bib-init bumps to 024.)_

> _(Deploy gộp 2026-05-10 — FEATURE-020 + FEATURE-022 ship cùng release `release/v1.7.2` (commit merge `b379b59` từ main + branch `5bib_admin_restructure_v1`). 5/5 prod containers update tới image `b379b59`. Live verify HTTP 200 cả public + admin + API stats. Deploy theo BR-DESIGN-15 cutover ngay (admin internal < 10 user, dễ rollback).)_

> _(2026-05-10 FEATURE-022 ✅ DEPLOYED — Admin Design Tokens Sweep giai đoạn 1 (commit `a708e88` trên `5bib_admin_restructure_v1` → merge main `b379b59`). Phương án C của Manager — token sweep trước, refactor incremental sau. 17 file (6 mới + 11 cập nhật): design-tokens.ts (22+ color tokens), fonts.ts (Be Vietnam Pro + JetBrains Mono qua next/font local), nav-groups.ts (4 NAV_GROUPS Vận hành/Nội dung/Hỗ trợ/Hệ thống), Sidebar.tsx (240px dark slate `#0F172A`), Topbar.tsx (60px white), GlobalSearch.tsx (placeholder modal qua @base-ui/react Dialog — full search defer F-023+), globals.css (Tailwind v4 @theme inline), root + dashboard layout, 8 shadcn ui components refresh (button, card, input, dialog, badge, table, tabs, select). Critical color decision: --primary đổi từ magenta `#FF0E65` sang blue `#1D49FF` (BR-DESIGN-03), magenta giữ làm --admin-magenta cho NEW badge / live indicator / brand moment. F-005/F-018/F-019 KHÔNG dùng `--primary` direct → SAFE. 15/15 BR-DESIGN cover. Build PASS 41/41 pages. Visual smoke 10 page lớn HTTP 200. Scope Lock divergence JUSTIFIED: repo dùng Tailwind v4 (KHÔNG có tailwind.config.ts) + chỉ có select.tsx (toast=sonner, dropdown/tooltip/avatar shadcn chưa có). 4 TD: GLOBALSEARCH-PLACEHOLDER + SIDEBAR-COUNT-PLACEHOLDER + VISUAL-PIXEL-PERFECT + TS-PRE-EXISTING-TESTS. Giai đoạn 2 sẽ tách F-023 → F-027 (Dashboard / Command Center hero / Master Data / Analytics deeper / Reconciliations + Team). KHÔNG chốt counter ở đây — F-023 reserved cho Dashboard giai đoạn 2.)_

> _(2026-05-10 FEATURE-020 ✅ DEPLOYED — Awards UX Top Chung Cuộc + nút đẩy trạng thái nổi bật (commit `37a6325` standalone main, merge gộp `b379b59`). EXTEND_EXISTING vá 2 lỗ hổng UX phát hiện sau F-019 v2.1 ship: Lỗ hổng A — Top Chung Cuộc biến mất sau mode mutually_exclusive (BIB top 3 overall bị exclude khỏi AG buckets nhưng KHÔNG persist vào podium nào → BTC không có chỗ tra cứu). Lỗ hổng B — Máy trạng thái 9 bước backend đầy đủ nhưng UI bị chôn sau "Mở rộng" của AGPodiumCard. Backend extend KHÔNG breaking: Schema Podium thêm `podiumType: 'AG'|'OVERALL'` default 'AG' index. Service awards.service.ts#recompute() upsert OVERALL doc per course song song AG. Helper computeOverallTopN tách thuần từ ag-bracket-calc — 6 unit test riêng. Admin chỉ frontend: OverallPodiumCard, PodiumStateMachineControls (tách từ AGPodiumCard), StateTransitionConfirmModal (simple 1-bước theo BR-AG-48), StateMachineProgressDots (visualizer 9-state). 14 file thay đổi (9 Scope Lock + 5 file justified ngoài scope: DTO + types + microcopy + AGPodiumGrid). Tests 91/91 PASS (6 helper + 85 awards regression). Pre-push CI parity gate 5/5 PASS. QC verdict 🟡 APPROVED WITH CAVEATS (0 CRITICAL, 0 NEW HIGH, 100% BR-AG-41 → BR-AG-50). 10 BR-AG mới: BR-AG-41 OVERALL cùng 9-state matrix với AG, BR-AG-42 persist OVERALL cả 2 modes, BR-AG-47 PODIUM_PUBLISHED nội bộ admin KHÔNG tự đẩy public, BR-AG-48 simple modal 1-bước, BR-AG-50 top N=3 cố định MVP. 3 TD: INTEG-TEST-DEFERRED + SDK-REGEN + SMOKE-TEST-PENDING (Danny verify race Cong An post-deploy).)_

> _(Bumped 2026-05-10 — FEATURE-022 INITIATED nhưng ĐỔI SCOPE: ban đầu định nghĩa "backend upload slugify Vietnamese filename + frontend sweep image encodeURI" (theo dõi tiếp F-021 Bug #5) NHƯNG Danny chốt HỦY scope cũ vì rủi ro lớn — admin chỉ cần đổi tên ảnh local thành ASCII trước upload là đủ, không sửa backend. F-022 mới = **Admin Design System Restructure** (phương án C của Manager — token sweep trước, refactor incremental sau) — áp dụng bộ admin-redesign Danny gửi (10 màn spec + JSX mockup tại `/Users/dannynguyen/Downloads/admin-redesign/`). Visual mới: stone warm bg + blue/magenta primary + JetBrains Mono cho data + sidebar 4 nhóm 240px dark slate `#0F172A`. Giai đoạn 1 = token sweep an toàn, giai đoạn 2 = 5 màn ưu tiên (Dashboard / Races list / Race Overview / Command Center / Reconciliations) tách thành FEATURE-023-027. Branch riêng `5bib_admin_restructure_v1` từ main. Cluster #9 (F-018 Y tế / F-019 Trao giải) Danny chấp nhận "phá" để áp visual mới. F-020 paralel trên main, KHÔNG block. 14 PAUSE-MGR critical liệt kê trong file 00, Danny commit "tao trả lời" trực tiếp. Next /5bib-init bumps to 023.)_

> _(Bumped 2026-05-09 — FEATURE-021 ✅ DEPLOYED prod release/v1.7.0 commit `49521b2`. Public Display Celebration BUGFIX P0 — 4 bugs nhạy cảm public layer race FUYU live: (1) `parseDistanceKm` regex `m\b` ăn `4800m` thành 4800 miles → 7724km → fake ULTRA badge; (2) `detectPodium*/AgePodium*/Ultra*` thiếu finished guard → vendor RaceResult OverallRank=1 pre-race fire "🥇 Vô địch" cho athlete chưa finish; (3) `getCourseStats(courseId)` thiếu `raceId` filter → cross-race data leak (banner FUYU 5km hiển thị 1.567 starters thay vì 15 thật, aggregate 195 races platform); (4) image URL không URL-encode → ảnh bìa + ảnh cự ly với tên file Vietnamese diacritics + spaces silent fail render CSS `background-image: url(...)`. Full BUGFIX workflow B sequential — Pre-push CI parity gate 5/5 PASS prevented F-019 4-round repeat. Files 14 (8 Scope Lock + 4 SDK auto + 2 downstream JUSTIFIED). Tests 91/91 badge + 18/23 race-result PASS. QC APPROVED WITH CAVEATS (0 CRITICAL/0 NEW HIGH). Live smoke verified — BIB 501 empty badges, 5km=15 + 7km=57 (khớp DB 72 total), old endpoint 404 (breaking change OK). 3 TD documented (TD-F021-PURGECACHE-PATTERN-LAG admin still uses old `stats:${courseId}` pattern; TD-F021-RACE-RESULT-SPEC-INFRA 5 pre-existing fails; TD-F021-IMAGE-SWEEP-INCOMPLETE 10+ sites homepage/calendar/ranking defer F-022). NEW BR-DISPLAY-11: image render PHẢI `encodeURI()`. NEW Cross-race scoping rule: mọi service đụng `race_results` PHẢI filter raceId trước. F-022 follow-up: backend upload service slugify Vietnamese filename (root cause Bug #5) + frontend sweep image encodeURI 10+ sites + helper `lib/url.ts` `safeImageUrl()`. Next /5bib-init bumps to 022.)_

> _(Bumped 2026-05-09 — FEATURE-020 INITIATED for Awards UX gap discovered post-F-019 v2.1 ship. Danny test local awards page race "Giải Công An" 10km và phát hiện 2 lỗ hổng: (A) v2.1 mode `mutually_exclusive` mặc định → top 3 overall (BIB 11009/14994/12397) bị EXCLUDED khỏi AG buckets nhưng KHÔNG persist vào podium doc nào → BTC không biết ai nhận medal Overall (12 medal/race × 4 course); (B) state machine 9-state forward-only đã có backend (`PATCH /awards/podium/:id/state`) nhưng UI buttons chôn sau "Mở rộng" của AGPodiumCard.tsx line 84-93 + thiếu confirmation modal cho irreversible LOCK/PUBLISH/FINAL + thiếu progress dots visualizer + thiếu batch action. EXTEND_EXISTING — extend AwardsModule (Race Ops Cluster #9 #3). Scope A backend: thêm field `podiumType: 'AG'|'OVERALL'` discriminator vào Podium schema + recompute() upsert OVERALL doc cùng lần với AG buckets, tách helper `computeOverallTopN` từ `ag-bracket-calc.service.ts:289-298`. Scope B admin only: refactor AGPodiumCard render PodiumStateMachineControls ngoài expanded block + new StateTransitionConfirmModal + StateMachineProgressDots. ZERO backend touch Scope B. 3 PAUSE-MGR critical: A1 OVERALL state matrix 9-state vs 4-state (recommend 9-state reuse), B2 confirmation modal pattern (recommend mix simple LOCK + 2-step typing PUBLISH/FINAL), A4 mode `compounding` có persist OVERALL không (recommend không persist). Next /5bib-init bumps to 021.)_

> _(Bumped 2026-05-09 — FEATURE-019 SHIPPED via 3-phase workflow v1 → v2 → v2.1 (commit `3f65c31` push `origin/5bib_racemonitor_v1`). Awards AG Podium + Anomaly Warnings — Race Ops Cluster #9 #2 (sau F-018 Medical Incident #9 #1). v1 implemented 53 files but UAT FAIL Giải Công An (vendor Category whitespace, 100% Path B fail, 0 podium → silent false negative). v2 strategic pivot: 5BIB independent calc Path A (DOB → ageOnRaceDay Option B isolation) + Pattern H VENDOR_MISMATCH cross-check, 29 files (13 NEW + 16 MODIFY), 72 tests PASS. v2.1 clarification: Danny chốt VN amateur convention top 3 overall EXCLUDED khỏi AG buckets, flip default `awardsCompoundingMode='mutually_exclusive'`, 6 files, 79 tests PASS (+2 v2.1). Total 80 files: 68 NEW + 12 MODIFY, 9139 insertions. QC verdict APPROVED WITH CAVEATS (0 CRITICAL/0 NEW HIGH, 1 HIGH inherited TD-F019-MULTITENANT pre-existing từ F-018). Live smoke Cong An verified top 3 overall EXCLUDED, BIB 11837 promoted rank 1 M_30-39. Skip counter F-017/018 (đã ship qua các batch trước; feature-log was stale). Next /5bib-init bumps to 020.)_

> _(Bumped 2026-05-08 — FEATURE-016 INITIATED for CRITICAL financial bugfix: reconciliation backend missing GROUP_BUY category in FIVE_BIB_CATEGORIES → recon underbills 5BIB fee for any race with group orders. Race 117 Cat Tien April 2026 case discovered by Danny — manual nhân viên 32,962,400 vs recon 22,596,000, diff = exactly 1 GROUP_BUY order 200026030. Full Option A workflow: code fix + 18 recon recompute migration + audit endpoint. Danny mandate "không sai là chết". Next /5bib-init bumps to 017.)_

> _(2026-05-08 FEATURE-015 ❌ ROLLED BACK same day — Check-In Kiosk SCRAPPED, duplicate of ORG.5bib.com pickup module (Vietnamese organizer admin platform — separate codebase). Surgical removal: 22 admin files + 8 backend files + 1 migration deleted. F-013/F-014/F-017 PRESERVED. `admin/src/lib/kiosk/` shared lib KEPT (used by F-013 + F-017). Race Ops Cluster #9 #1 slot REOPENS — candidates Volunteer Hub / Medical Incident / BIB Assignment per Strategic Scout. TD-F005-01 REVERTED to OPEN (ORG.5bib.com is source of truth). Lesson: Strategic Scout MUST scope ALL 5Solution platforms (5bib + ORG + 5sport + 5pix + 5tech), not just current repo. Counter unchanged.)_

> Feature counter for next /5bib-init.

> Manager tăng counter mỗi khi `/5bib-init`. Format: `FEATURE-001`, `FEATURE-002`, …

---

## 📋 Shipped features (qua workflow này)

| Date | ID | Title | Type | Files đụng | Risk | Note |
|------|-----|-------|------|-----------|------|------|
| 2026-05-16 | FEATURE-038 | Finance Contracts List with P&L Per Row | BUGFIX + EXTEND_EXISTING (F-028 Phase 2 follow-up) | 15 file Scope + 1 regression test assert: Backend 8 (3 NEW: `pnl-contracts-list-filter.dto.ts`, `pnl-contracts-list-response.dto.ts`, `pnl-contracts-list.controller.ts`; 5 modify: `pnl.service.ts` +5 methods, `pnl.service.spec.ts` +14 tests, `finance.module.ts` register, `cost-items.service.ts` flush dual-pattern, `contracts.service.ts` flush dual-pattern); Admin 7 (5 NEW components: `contracts-list-client.tsx`, `contracts-list-table.tsx`, `contracts-list-footer-summary.tsx`, `margin-legend-banner.tsx`, `contracts-list-empty-state.tsx`; 2 modify: `finance-api.ts` add helper+types, `contracts/page.tsx` REWRITE placeholder); +1 `cost-items.concurrency.spec.ts` assertion 2→4 (BR-38-09 dual-pattern flush necessary consequence). | 🟡 MED perf + 🟢 LOW backend/admin | Replaced F-028 Phase 1 placeholder `/finance/contracts` page. Endpoint NEW `GET /api/finance/pnl/contracts` paginated reuse `getDashboardData()` compute path no-slice. 11 BR-38-* + 11 TC-CL-01..11 + 10 E2E + 7 UI states. Status whitelist BR-PNL-08 strict ACTIVE+COMPLETED inherited. Search combined contractNumber+entityName+raceName regex-escaped (ReDoS defense). Sort default anchorMonth DESC + 5 sortable cols. Pagination 20/50/100. Margin legend 🟢🟡🔴⚪. Cache `pnl:contracts-list:<sha256-16char>` TTL 60s. URL deep-link `?period=&page=&sortBy=...`. Admin defense-in-depth: backend LogtoAdminGuard + page `isAdmin` gate. 46/46 pnl.service.spec PASS (32 cũ regression OK + 14 mới). 250/250 broader finance+contracts. QC ✅ APPROVED 0 CRITICAL/HIGH (13/13 vectors mitigated incl regex escape + no PII + no IDOR admin-only). 8 TD-F038-*: SDK-REGEN, REFACTOR-EXTRACT (~80 LoC duplicate computeContractRows ↔ getDashboardData body — deliberate copy zero regression), MONGO-SORT (acceptable <1K), EXPORT-LIST (defer Phase 2 PAUSE-38-06), FILTERED-COST-CATEGORY (unused on list page), PAGE-CLAMP, AUTH-INTEGRATION-TEST (HTTP 401/403 via E2E walkthrough), PERF-SLA-MEASURE (cần đo empirical p95). **NEW patterns minted (added to conventions.md):** (1) Dual-pattern cache flush helper iterates `[A, B]` series; (2) URL deep-link 2-level debounce (local 0ms + URL 400ms); (3) Defense-in-depth admin gate (Guard + page `isAdmin` before client mount). NOT YET pushed/deployed PROD — branch local trong worktree `funny-kirch-90e777`. Danny next: Manager+BA empirical UI walkthrough localhost docker-compose restart admin với code mới, verify SLA + persona journeys → push để CI deploy. |
| 2026-05-13 | FEATURE-032 | Partner Excel Import | EXTEND_EXISTING (F-024) | 13 file: 3 BE NEW (`import-partner.dto.ts`, `partners-import.service.ts`, `.spec.ts`) + 3 BE modify (`partners.controller.ts`, `contracts.module.ts`, `partners.service.ts`) + 1 admin NEW (`partner-import-dialog.tsx`) + 2 admin modify (`partners/page.tsx`, `lib/contracts-api.ts`) + 4 workflow docs | 🟢 LOW | Pattern reuse 100% F-031 — Danny prompt "thêm cả tính năng import đối tác nữa, tao quên mất". 7 PAUSE-32-* chốt option "a" auto-confirm F-031 defaults: 11 cols VN headers, dual-key dedup taxId primary + entityName fallback (PAUSE-32-02 mới — MST stable hơn tên), 2-step Preview→Confirm, Max 200, createdBy=userId admin, Empty per-row skip+collect, CÓ template. Server RE-VALIDATES at confirm step (TC-IM-07 race mitigation). EMAIL_RE strict `/^[^\s@]+@[^\s@]+\.[^\s@]+$/` reject `foo@bar`. ExcelJS reuse F-031 pattern verbatim. FileInterceptor 5MB reuse. 9/9 NEW TC-IM-* PASS + 5/5 partners.service.spec regression = 14/14 PASS. QC ✅ APPROVED 0 CRITICAL/HIGH. 3 TD-F032-* LOW: SDK-REGEN (admin raw fetch), VN-DIACRITIC (no NFD normalize), RELEASE-BRANCH (resolved at deploy — fast-forward release/v1.8.0 onto origin/main caught F-030 + F-031). ETA ~1h actual (vs ~2h est) — pattern proof compounding. Commit `46563e3` on release/v1.8.0 (PROD) + main (DEV). Cũng minted observation: release branch maintenance lesson — Manager phải verify release branch ≤ main HEAD trước commit feature mới, else PROD deploy lacks predecessor commits. |
| 2026-05-13 | FEATURE-031 | Service Catalog Excel Import | EXTEND_EXISTING (F-024) | 9 file: 3 BE NEW (`import-service-catalog.dto.ts`, `service-catalog-import.service.ts`, `.spec.ts`) + 3 BE modify (`service-catalog.controller.ts`, `contracts.module.ts`, `service-catalog.service.ts`) + 1 admin NEW (`service-catalog-import-dialog.tsx`) + 2 admin modify (`service-catalog-table.tsx`, `lib/contracts-api.ts`) | 🟢 LOW | Danny report admin "Hợp đồng → Danh mục dịch vụ" cần bulk import Excel. 7 PAUSE chốt: 7 cols VN headers, Duplicate Skip+report, 2-step Preview→Confirm, Max 200, createdBy=userId, Empty per-row skip+collect errors, CÓ template download. Backend 3 endpoints `POST /import-excel/preview` + `/confirm` + `GET /import-template` literal BEFORE `:id` (F-021 convention). Server RE-VALIDATES duplicate at confirm (race mitigation TC-IM-09). Category parser accept VN labels (Tính giờ/Racekit/Vận hành/Chung) + English enum case-insensitive. ExcelJS reuse từ reconciliation/xlsx.service. FileInterceptor 5MB reuse F-024 contract-templates. 9/9 NEW TC-IM-* PASS + 16/16 service-catalog domain regression. QC ✅ APPROVED 0 CRITICAL/HIGH. 5 TD-F031-* all LOW/MED. Rebase trên top F-027 Promo Hub merge — clean fast-forward push. Commit `6e30ef9` on main → `release/v1.7.9` → CI auto-deploy PROD. **NEW pattern minted:** "2-step Excel Import UX" (Preview→Confirm với badges Hợp lệ/Trùng/Lỗi) — reusable cho future bulk import (athletes, sponsors, partners). |
| 2026-05-13 | FEATURE-027 | Promo Hub Configurable Landing Page (19 section types) | NEW_MODULE | ~66 files: backend `promo-hub/*` + `promo-hub-analytics/*` (20 NEW Phase A1: 2 modules, 5 schemas, 9 DTOs, 4 services/controllers, 3 spec = 28/28 PASS), admin `(dashboard)/promo-hub/*` (list/new/[id] + 8 components incl `PromoHubAnalytics.tsx` AreaChart reused + `section-types.ts` 19 SECTION_TYPE_META entries), admin `lib/nav-groups.ts` Sparkles entry + `app/api/revalidate-hub/route.ts`, admin + frontend SDK regen, frontend `app/(main)/hub/[slug]/` SSR page + not-found + `components/hub/PromoHubRenderer.tsx` dispatcher + `PromoHubTracker.tsx` client (DOM event delegation) + 19 section components (9 Phase A + 10 Phase B), `app/sitemap.ts` extend, `app/api/revalidate-hub/route.ts` cross-app revalidate (token-gated). Branch `5bib_promo_hub_v1` 5 commits off `a638b28` (post-F-029): `0d8d1fa`+`3baef65`+`be3f469`+`ac13c51`+`84564d6`. | 🟡 MED (NEW MODULE biggest sau F-024/F-028 — independent, ZERO cross-module Nest DI from existing — sections fetch races/sponsors/results trực tiếp tại SSR layer KHÔNG qua RacesService/SponsorsService DI) | iRace AddMe.vn Linktree kill strategy. Phase A1-A5 ship 9 base types + admin editor + public SSR + analytics charts + cross-app revalidate; **Phase B post-Danny iRaceticket gap analysis** ship 10 thêm (link_grid + social_links + faq + countdown + video_embed + image_gallery + testimonial + map_embed + schedule_timeline + form_embed). **19/19 types covered** admin form + public render + dispatcher. 16 BR-PH-* + 1 BR-PH-17 (Phase B) verified. 12+8=20 security threats mitigated incl IP SHA-256 hash GDPR-compat, rate-limit SETNX 5min/IP/slug (BR-PH-09), sanitize-html rich text + customCss (BR-PH-12), HOST whitelist iframe (map_embed `google.com`/`openstreetmap.org`; form_embed `docs.google.com`/`forms.gle`/`tally.so`/`form.5bib.com`/`forms.office.com`), youtube-nocookie no-tracking, regex extract YouTube/Vimeo ID, fail-closed REVALIDATE_TOKEN. Performance: TTFB 37ms cached / 59ms với 15 Phase A+B sections (vs PRD ≤500ms = 8-13x faster). MongoDB indexes auto-create: `slug` sparse unique, `{status, createdAt:-1}` compound, `{hubId, clickedAt:-1}` + TTL `expireAfterSeconds: 7776000` (90d). Redis NEW namespace: `promo-hub:<slug>` 60s, `promo-hub-lock:<slug>` 5s SETNX (anti-stampede port F-004 RaceMasterData), `promo-hub-view-rl:<slug>:<ipHash>` 5min. **8 NEW patterns minted (added to conventions.md):** anti-stampede SETNX port, section-as-subdoc array, DOM event delegation tracker, HOST whitelist iframe, inline SVG brand icons (lucide v1.7 gap), cross-app revalidate proxy + Bearer token, Next.js 16 `revalidateTag(tag, profile)` 2-arg signature, switch-over-registry type dispatch. **PROD ENV preconditions:** Set `REVALIDATE_TOKEN` + `FRONTEND_REVALIDATE_URL` (per `OPS-NOTES.md`) — without set: feature works, propagation falls back to ISR 60s (acceptable graceful fallback). **Workflow lessons (added to conventions.md):** (1) BA-to-PRD competitor cross-check gap — PRD BR-PH-04 chốt 9 sections without checking real-world Linktree/addme.vn parity → Phase B addendum post-QC; Manager process fix: NEW_MODULE phải yêu cầu BA list 2-3 competitor URLs + section parity check trước APPROVE plan. (2) Phase B shipped without re-running `/5bib-prd` — acceptable ONE-OFF for pure visual variant additions (no new BR), format anomaly documented. **PENDING:** Danny approve push branch + merge main + PROD env config. Branch local, KHÔNG tự push theo Danny mandate "không được merge main khi chưa được phép". |
| 2026-05-13 | FEATURE-030 | Reconciliation Add-on Visual + 5BIB Provider Config | BUGFIX + EXTEND_EXISTING | 8 file (5 Scope Lock + 3 test artifacts): backend `src/config/index.ts` extend (Joi 8 `PROVIDER_*` + env.provider namespace fail-soft `.default()`), `reconciliation-calc.service.ts` add_on_price dedup fix, `xlsx.service.ts` render col 6 add_on per-line + bottom Tổng include, `docx.service.ts` import env replace 6 hardcoded refs + signature + conditional "Vật phẩm bổ sung" row, 3 NEW spec (`reconciliation-calc.service.spec.ts` 5 TC + `xlsx.service.spec.ts` 1 TC + `docx.service.spec.ts` 4 TC), `.env.example` 8 PROVIDER_* vars. | 🟢 LOW | Discovered via Danny PROD report Zaha Hai Phong tháng 4: (1) XLSX Section 1 vs Section 3 vênh 299K = add-on orphan; (2) 5BIB info hardcode SAI từ legacy `205a1c1`. Sub-bug discovered: `total_add_on_price` order-level field cộng dupe per-row (potential over-count với 1 order multi-line items) — fix DEDUP by order_id Set giống pattern `discount_amount`. 5 PAUSE-30-* chốt: Option 2 visual fix (KHÔNG đổi gross/fee — add-on vẫn tính phí 5.5%), KHÔNG migrate cũ (F-025 bulk delete xóa thủ công), env config option A, 7 fields đầy đủ. 10/10 NEW TC-AO-* PASS. Math Zaha fixture: Section 1 18,422,200 = Section 3 (18,123,200 line items + 299,000 add-on). 8/9 reconciliation domain suites PASS (controller.spec fail pre-existing F-029 LogtoStaffGuard refactor — flagged TD-F029-INHERITED-CTRL-SPEC). 5 TD-F030-* flagged all LOW. Commit `f980228` on main → `release/v1.7.8` push → CI auto-deploy PROD. **NEW pattern minted:** "Fail-soft env defaults" (Joi `.default()` cho business legal info thay vì `.required()` — KHÔNG outage container restart, deploy resilient). BA gate SKIPPED (scope hẹp + 5 PAUSE chốt rõ — Manager Plan thay PRD pattern proven F-025 + F-030). **Lesson learned:** Danny mandate "đừng tự quyết định" — Manager phải hỏi business info chính xác trước hardcode default. |
| 2026-05-13 | FEATURE-029 | Hardening Phase 1 + Phase 1.1 — HIGH non-CRIT batch (HIGH-RR-01 + HIGH-PERF-01 + HIGH-RBAC-01 + Display sweep) | REFACTOR + BUGFIX | ~65 files: backend (race-result service + controller + module, finance pnl + fee services, logto-auth `permissions.helper.ts` NEW) + 37 admin pages RBAC wrap (33 isStaff + 4 isAdmin) + 9 display sweep + `admin/src/lib/timing-labels.ts` NEW + 16 spec extend + 6 SDK regen auto. Phase 1.1 adds 13 sibling race-result endpoints draft filter via `enforceRaceVisibility` + `resolveRaceIdFromCourseId` extracted helpers. | 🟡 MED | Branch `5bib_hardening_phase_1_v1` off main `01c2950`. **38/38 F-029 unit tests PASS** (24 race-result + 6 pnl + 8 fee). 0 new typecheck error (4 pre-existing Vitest upload spec). QC v2 ✅ APPROVED no PROD BLOCKER — TD-F029-NEW-01 RESOLVED. 5 PAUSE-MGR-* compliance (1 resolved workspace switch + 4 deferred testable post-deploy). Workflow exception: PRD BR-HD-01 implicit scope extended via Manager Plan Phase 1.1 extension (BA artifact untouched — pure defense-in-depth completion). 7 TD remained: TD-F029-NEW-02 POST result-image ungated + TD-F029-NEW-03 global search leak (both defer FEATURE-030 Wave 2) + TD-F029-01..05 carryovers (EXPLAIN ANALYZE bulk SQL, Skeleton screenshot diff, redirect-only TM pages, merchants raw enum, pre-existing test infra debt). 5 CRIT (TD-2026-05-12-CRIT-*) remain Danny-deferred ("dev có lý do"). **NEW pattern minted:** `enforceRaceVisibility(raceId, user?)` public helper extracted in race-result.service.ts — reusable from controller for endpoints bypass standard public methods. Pattern reusable cho future feature gate public endpoints by mutable resource state. **NEW pattern minted:** RBAC page-level gate via `useAuth().isStaff/isAdmin` + conditional render `<RestrictedAccess />` — defense-in-depth (sidebar hide + backend guard + page-level gate = 3 layers). Applied to 37 admin pages. **NEW pattern minted:** Dual-check permission helpers `isStaffOrHigher(user)` + `isAdminOrHigher(user)` in `logto-auth/permissions.helper.ts` — mirrors LogtoStaffGuard + LogtoAdminGuard hierarchy verbatim. PROD release pending: branch ready to merge → cut `release/v1.8.0` (Danny + DevOps decide timing). |
| 2026-05-12 | FEATURE-028 | Deal P&L Tracking Module | NEW_MODULE | backend `finance/*` (pnl.service, cost-items, fee.service cross-DB MySQL, pnl-excel) + `contracts/schemas/contract.schema.ts` (catalogItemId + linkedTenantId/MysqlRaceId sparse-indexed) + admin `(dashboard)/finance/*` (dashboard P&L, period-filter, contracts list, cost-items CRUD) + admin `(dashboard)/contracts/_components/service-catalog-picker.tsx` rewrite Table 6-col + `admin/src/lib/finance-labels.ts` central VN dictionary | 🔴 HIGH | 3 phases: Phase 1 backend P&L compute + cost items + cross-DB MySQL FeeService (F-016 pattern via `order_metadata → order_line_item → ticket_type → race_course` JOIN, KHÔNG `o.tenant_id` — column không tồn tại); Phase 2 dashboard aggregated + period filter (5 preset + custom); Phase 3 cost suggestions (catalog reference cost × quantity) + margin badge. BR-PNL-08 strict whitelist `status: $in: ['ACTIVE','COMPLETED']` (Danny chốt "chỉ deal đã chốt"). Critical bugs phát hiện trong dev: (1) MySQL `o.tenant_id` not exists — fix removed filter race_id unique per tenant; (2) backend serving cũ DTO sau rebuild — fix kill PID 98258+19555 force rebuild; (3) infinite render loop `useSetCrumb` 2694 console errors — fix useRef pattern; (4) raw enum `last_3_months` render — fix force `{PERIOD_LABEL[period]}` children SelectValue; (5) title `Lãi/Lỗ Deal · 407db4` slice(-6) ObjectId — fix contractNumber fallback. Display Convention rule ban hành: KHÔNG render raw enum/snake_case cho user (commit `f18da46`). Redis keys mới `pnl:contract:*` + `pnl:dashboard:*`. KHÔNG có `05-manager-deploy.md` — Danny merge main `01ad950 → 01c2950` ad-hoc trước ultrareview. |
| 2026-05-11 | FEATURE-024 | Contract Management Module | NEW_MODULE | backend `contracts/*` (controllers, services, schemas: contract, partner, service-catalog, contract-template, lifecycle event), `audit/*` integration, `upload/*` DOCX template upload + magic-bytes MIME, `utils/build-filename.ts`, `scripts/inject-template-placeholders.py`; admin `(dashboard)/contracts/*` (~30 files: list, detail, partners CRUD, service-catalog table 6-col, templates editor TipTap + DOCX viewer mammoth, lifecycle actions); shared `lib/auth-context.tsx` extended `isAdmin/isStaff/hasScope/hasRole`; S3 lifecycle rule 6 added (5y retention) | 🔴 HIGH | NEW MODULE biggest sau team-management. DOCX template gen với docxtemplater + pizzip + libreoffice-convert PDF. RBAC implement với Logto 3 permissions (all/admin/staff), `LogtoStaffGuard` new, 12 controllers refactored. Cross-DB MySQL Tenant/Race picker cho TICKET_SALES contracts linking (`linkedTenantId/linkedMysqlRaceId` sparse-indexed). F-024 root cause bug: ContractsModule placed inside `volunteerDbModules` conditional → only loaded với VOLUNTEER_DB_HOST env → backend serving fail smoke. Fix: move out of conditional (commit `6a26511`). DOCX format preservation bug: Coder Phase 2B `gen-contract-stub-templates.ts` tạo DOCX from zero 17KB mất 100% format vs mẫu gốc 4.5MB → fix python-docx inject `{placeholder}` preserving format (commit `e43fd77`) + `strip_embedded_fonts()` reduce 30MB → <50KB. Filename convention `[Provider] [Partner] - [DocType] [Service] - [DD.MM.YYYY].ext`. KHÔNG có `05-manager-deploy.md` — Danny merge main ad-hoc. |
| 2026-05-10 | FEATURE-026 | Admin Analytics Redesign | EXTEND_EXISTING | backend `analytics/*` (controllers, services, period-compare aggregation, repeat-trend formula fix); admin `(dashboard)/analytics/*` (revenue trend chart, repeat customer analytics, period compare selector, segment heatmap) | 🟡 MED | TD-F026-REPEAT-TREND-FORMULA fix corrected per memory. TD-F026-CACHE-INVALIDATE pre-existing. Phase 2 admin redesign batch 1 cùng F-023 (push main commit `c0d799e` branch `5bib_admin_dashboard_v1`). |
| 2026-05-10 | FEATURE-023 | Admin Dashboard Redesign | EXTEND_EXISTING | backend `dashboard/*` (homepage stats, recent-activity, KPIs); admin `(dashboard)/page.tsx` redesign + components stats-cards, activity-feed, quick-actions | 🟡 MED | 24 files Scope Lock. Phase 2 redesign giai đoạn 2 màn 01. TD-F023-* tracked. |
| 2026-05-10 | FEATURE-022 | Admin Design Tokens Sweep | REFACTOR | `admin/src/lib/design-tokens.ts` central token registry; ~317 admin `.tsx` files sweep colors/spacing/radii/typography; image URL encodeURI helper `lib/url.ts` `safeImageUrl()` | 🟢 LOW | Giai đoạn 1 token sweep (commit `a708e88` branch `5bib_admin_restructure_v1` merged `b379b59`). F-021 follow-up: backend upload service slugify Vietnamese filename + sweep 10+ sites image URL encode (homepage/calendar/ranking). |
| 2026-05-10 | FEATURE-020 | Awards UX Overall + State Actions | EXTEND_EXISTING | backend `awards/*` (podium-overall service); admin `(dashboard)/races/[id]/awards/*` (top chung cuộc tab + state transition buttons nổi bật + dispute hearing UI) | 🟡 MED | Awards UX Top Chung Cuộc + nút đẩy trạng thái nổi bật (commit `37a6325` standalone main, merge gộp `b379b59`). F-019 follow-up improvements. |
| 2026-05-09 | FEATURE-021 | Public Display Celebration BUGFIX | BUGFIX | backend `badge.service.ts` (parseDistanceKm regex fix `m\b` → 4800m parsed correctly, detectPodium/AgePodium/Ultra finished guard), `race-result/getCourseStats` raceId scoping filter, frontend image URL `encodeURI()` sweep | 🔴 HIGH | 4 bugs P0 PROD race FUYU live: (1) `parseDistanceKm` regex ăn 4800m thành 4800 miles → 7724km fake ULTRA badge; (2) podium detect thiếu finished guard → vendor RaceResult OverallRank=1 pre-race fire "🥇 Vô địch" cho athlete chưa finish; (3) `getCourseStats(courseId)` thiếu `raceId` filter → cross-race leak (FUYU 5km hiển thị 1.567 starters thay vì 15 thật, aggregate 195 races); (4) image URL không URL-encode → ảnh tên file Vietnamese diacritics + spaces silent fail render. Full BUGFIX workflow B. PROD release/v1.7.0 commit `49521b2`. NEW BR-DISPLAY-11 + Cross-race scoping rule. 3 TD-F021-* tracked. |
| 2026-05-08 | FEATURE-018 | Medical Incident Tracker | NEW_MODULE | backend `medical-incidents/*` (10 endpoints, state machine TRIAGED→TRANSPORTED→RESOLVED, severity 1-5, S3 photo + report PDF signed URLs); admin `(dashboard)/races/[id]/medical/*` (incident tab, state transitions, photo upload, dispute hearing) | 🟡 MED | 10 endpoints `LogtoStaffGuard`. Redis keys `medical:race:<raceId>:active-count` + `medical:incident-lock:<incidentId>` SETNX 5s anti-race. S3 lifecycle rule 4 added (7y legal retention VN Civil Code Art. 588-589 + ITRA + insurance). EXIF strip post-upload (timestamp + gps only). MIME allowlist image/jpeg/png/webp max 10MB. PII anonymization cron stripping athleteName/bib/description sau 7y. KHÔNG có `05-manager-deploy.md` — ship qua main ad-hoc. |
| 2026-05-08 | FEATURE-017 | Result Kiosk Chip Scan Config | EXTEND_EXISTING | backend `result-kiosk-display/*` (sponsor logos config, kiosk template), `chip-verification/*` (config CRUD, mapping, ChipThrottlerGuard custom 60/min token-based ratelimit); admin `(dashboard)/races/[id]/result-kiosk/*` + `/chip-verification/*` | 🟡 MED | S3 lifecycle rule 3 added (`result-kiosk-sponsors/` indefinite, max 2MB × 5 logos/race). ChipThrottlerGuard pattern reusable. KHÔNG có `05-manager-deploy.md` — ship qua main ad-hoc. |
| 2026-05-08 | FEATURE-016 | Reconciliation Include Group Buy | EXTEND_EXISTING | backend `reconciliation/*` (FIVE_BIB_CATEGORIES update exclude MANUAL, fee.service.ts cross-DB MySQL pattern via `order_metadata → order_line_item → ticket_type → race_course` JOIN), `finance/*` (fee config) | 🟡 MED | Cross-DB DI pattern established (Finance → Reconciliation read-only via `@Optional()` MaybeUndefined). FIVE_BIB_CATEGORIES exclude MANUAL — MANUAL dùng VNĐ/vé, không phải %. Tenant repo + MerchantConfig schema cross-DB. F-028 sau này reuse exact pattern. KHÔNG có `05-manager-deploy.md` — ship qua main ad-hoc. |
| 2026-05-07 | FEATURE-008 | Command Center Refactor v2 + F-005 Migration | REFACTOR | backend `command-center/*` (full F-005 migration legacy `dashboard-snapshot:` → `master:rr-snapshot:<raceId>` 15s + `master:cc-leaderboard:<raceId>:<courseId>:<limit>` 15s + `master:cc-refresh-lock-user:<userId>` 30s BR-CC-10 Tier 1 per-user UX rate-limit); admin `(dashboard)/races/[id]/command-center/*` | 🟡 MED | v1 archived `_archive/v1-canvas-fidelity/` (5 files, QC PASS but not deployed — code v1 in-tree, v2 builds upon). v2 production-ready leaderboard live per course. |
| 2026-05-11 | FEATURE-025 | Reconciliation Bulk Delete | BUGFIX + EXTEND_EXISTING | 6 file (1 new DTO + 1 new DTO spec + 3 BE modify + 1 admin modify): backend `dto/delete-batch.dto.ts` NEW + `dto/delete-batch.dto.spec.ts` NEW (QC artifact) + `reconciliation.controller.ts` modify (`@Post('delete-batch')`) + `reconciliation.service.ts` modify (`deleteMany()` + Logger.warn audit) + `reconciliation.service.spec.ts` extend (TC-DM-* + TC-QC-DM-*) + admin `reconciliations/page.tsx` modify (button + Dialog + handleBulkDelete) | 🟢 LOW | Discovered via Danny PROD report "chọn vài đối soát ấn xóa → lỗi 500 lần đầu, lần 2 chỉ xóa 1". 2-layer fix: ROOT 500 = admin proxy 204 TypeError → hot patch `bab4c44` v1.7.5 (3 proxy sweep); FEATURE GAP = bulk delete UI/endpoint chưa có. F-025 build proper bulk: `POST /reconciliations/delete-batch` Mongoose `deleteMany({_id:{$in:ids}})` 1 RTT + admin button "Xóa hàng loạt (N)" + Dialog 2-button ZERO friction (Danny: "không bắt tao làm nhiều"). 5 PAUSE-25-* chốt: ZERO friction, KHÔNG status guard ("admin làm đéo gì chả được"), Logger.warn đủ, S3 lifecycle 24h, max 50 IDs. 24/24 NEW tests (5 Coder TC-DM + 3 QC TC-QC-DM + 9 QC TC-DT + 7 regression). 82/82 reconciliation domain regression. 0 break F-003/F-004/F-016. QC ✅ APPROVED 0 CRITICAL/HIGH/MED security threats. BA gate SKIPPED — scope hẹp + 5 PAUSE chốt rõ, Manager Plan thay PRD. Commit `84155aa` on main → `release/v1.7.7` push → CI auto-deploy PROD. 5 TD-F025-* flagged (SDK-REGEN re-run post-deploy, RAW-FETCH cosmetic, FE-UNIT-TEST block TESTSTACK, AUDIT-LOG-COLLECTION future compliance, SERVICE-LOGGER-INCONSISTENCY). **Conventions impact:** NEW pattern minted "Bulk delete idempotent return shape" — Mongoose `deleteMany({_id:{$in:ids}})` + return `{deleted, not_found}` counts thay vì throw 404, consistent với F-016 v1.6.5 idempotent UX, frontend handle both branches via secondary toast. Pattern reusable cho bất kỳ bulk admin action future (race, athlete, sponsor, contract, etc.). |
| 2026-05-09 | FEATURE-019 | Awards AG Podium + Warnings (v2.1) | NEW_MODULE | awards/, race-master-data/, races/, admin/awards | 🟡 MED | 5BIB independent calc + 2-layer verify + VN amateur convention default |
| ~~2026-05-08~~ INVALID | FEATURE-015 | Check-In Kiosk standalone (BIB pickup) (Race Ops Cluster #9 #1) | NEW_MODULE | ~50 files: 22 NEW admin `check-in-kiosk/` (page.tsx + 13 components + 4 hooks + 3 modules + 1 test folder); 6 NEW admin `lib/kiosk/` shared lib (useFullscreen + useKioskIdle + useKioskSound + kiosk.constant + types + index — extracted F-013 hooks generalized Option 3, drop "Kiosk" prefix); 8 NEW backend `race-result/check-in*` (controller + service + sse-service + module + log schema + 2 DTO + spec); 1 NEW migration `2026-05-08-add-check-in-window.ts` (PAUSED awaiting Danny prod sign-off); 1 NEW admin `checkin.types.spec.ts` runtime guard (23 cases EXECUTED PASS); 11 deferred Jest+RTL specs (`@ts-nocheck` F-013 pattern); 1 MODIFY `backend/src/app.module.ts` register CheckInModule; 1 MODIFY `backend/src/modules/races/schemas/race.schema.ts` add `checkInWindow: { start: Date, end: Date }` field; 1 MODIFY `admin/src/components/race-ops-shell/RaceTabsNav.tsx` add tab #10 Check-In Kiosk; 1 MODIFY `admin/jest.kiosk.config.cjs` extend testRegex; 1 NPM install `@zxing/browser@0.2.0` pinned; 1 SDK regen `admin/src/lib/api-generated/sdk.gen.ts` auto-regen via `pnpm --filter admin generate:api` | 🔴 HIGH | ❌ ROLLED BACK 2026-05-08 — duplicate of ORG.5bib.com pickup module. Surgical removal: 22 admin files + 8 backend files + 1 migration deleted. F-013/F-014/F-017 PRESERVED. `admin/src/lib/kiosk/` shared lib KEPT (used by F-013 + F-017). `@zxing/browser` package KEPT (F-017 may use Phase 2). `race.checkInWindow` schema field LEFT unused (no migration; will hard-delete on next breaking schema change). TD-F005-01 placeholder `racekit_received = 0` REVERTED to OPEN — ORG.5bib.com is source of truth, 5bib-result reads only. **Lesson learned:** Strategic Scout must scope across ALL 5Solution platforms (5bib + ORG + 5sport + 5pix + 5tech), NOT just current repo. Cluster #9 #1 slot reopens for re-allocation. **[Original SHIPPED note preserved below for historical reference:]** **Race Ops Cluster #9 OPENS with F-015 SHIPPED — first feature of "Race-Day Chaos Killer" cluster.** Standalone kiosk for BIB pickup at race venue (BTC tent registration); replaces no prior feature (NEW_MODULE). **TD-F005-01 RESOLVED** — `racekit_received` field finally written to MongoDB by F-015 atomic mutation (Cluster #4 placeholder always-0 lived ~3 weeks; closes via direct write, not via F-005 dashboard-snapshot patch). **Multi-station race condition mitigation VERIFIED** — Redis SETNX `checkin:lock:{raceId}:{bib}` 5s TTL distributed lock + atomic `findOneAndUpdate({raceId, bib, racekit_received: false})` two-tier guard. SETNX returns 0 → 409 `CHECKIN_LOCK_HELD`; matchedCount=0 + athlete exists → 409 `CHECKIN_ALREADY_PICKED_UP` (first-wins, 3s cooldown re-enable). NEW pattern reusable for any feature where multiple admin clients can mutate same resource (Volunteer Hub assignments, Medical Incident dispatch). **chip-verification boundary CLEAN** — BR-CK-20 grep audit zero actual imports (only docstring boundary disclaimers in 2 files). 2 modules cover overlapping problem domain (chip-verification = chip↔BIB tech check; check-in-kiosk = BIB pickup workflow); explicit BR + grep audit prevents accidental coupling. **CMND PII boundary CLEAN** — BR-CK-08/10 grep audit zero log statements / zero DB persistence of full CMND. last-4-digit visual match by BTC, NEVER stored. `cmnd_last_4` lookup uses anchored regex `^[0-9]{4}$` against master-data `RaceMasterData.cmnd_last_4` (master-data layer populated upstream by athlete sync; check-in writes nothing). `check_in_logs` schema stores ObjectId only — NO CMND, NO name (BR-CK-15). **F-013 hook port ACCEPTED-with-TD-F015-01** — Manager Plan §3 Option 3 generalized rename (`useKioskFullscreen` → `useFullscreen`; `KIOSK_CONFIG` → `SHARED_KIOSK_CONFIG`); logic equivalence preserved (same Fullscreen API + body data attr primitive, same idle setInterval 1Hz countdown, same Web Audio OscillatorNode beep, same localStorage SOUND_LS_KEY persistence). NOT byte-for-byte (rename + comment rewrite REQUIRED for cross-feature reuse). F-013 retrofit deferred TD-F015-01 (1-line `import` swap × 3 files; convenience, not blocking). **Multi-station SSE realtime sync** via NestJS `@Sse()` decorator + RxJS Subject + 25s heartbeat (mirrors F-005 timing-alert pattern; per-race filter via Subject filter). NEW pattern formalized for multi-tablet kiosks (4-8 tablets at race tent broadcast pickup events to all stations). **Test execution: 7/7 backend + 37/37 admin = 44/44 PASS, 0 fail.** Backend `check-in.service.spec.ts` covers: confirmPickup happy path atomic update + audit log + SSE broadcast / Redis lock held → 409 / matched=0 → 409 / athlete missing → 404 / invalid race id / lookupByCmndLastFour rejects non-4-digit + accepts 4 digits anchored regex. Admin `checkin.types.spec.ts` runtime guard 23 cases (`isAthleteCheckInResponse(x): x is AthleteCheckInResponse` validator following F-013 pattern); regression `kiosk.types.spec.ts` 14 cases preserved. Configuration: `jest.kiosk.config.cjs` extended regex matches both — F-013 + F-015 runtime guards executable via `backend/node_modules/.bin/jest` path-reuse (NO admin RTL stack install needed; TD-F013-TESTSTACK remains open). 11 frontend specs DEFERRED via Manager STOP — same `@ts-nocheck` F-013/F-014 pattern; 1-line `testRegex` flip activates when admin gains RTL stack. **22/22 BR-CK rules covered** (7 EXECUTED via Jest + 15 verified via static review). **Migration `2026-05-08-add-check-in-window.ts` PAUSED** — `DRY_RUN=1` env-flag mode for staging dry-run; idempotent (only writes where `checkInWindow` undefined/null AND `startDate` valid Date); formula `start = startDate - 3 days`, `end = startDate - 1 hour` per BR-CK-06; PAUSE awaiting Danny prod sign-off + staging run (TD-F015-03 HIGH pre-deploy gate). **Load test PAUSED** TD-F015-09 (50 concurrent/min × 10min sustained on staging — pre-deploy operational gate per Cluster #9 success criterion #5). **Race Ops 10-tab shell** (was 9 since F-008v2; F-015 makes it 10) — tab precedent overflow-later policy (option A — refactor "More" dropdown when shell >12 tabs). **Architecture impact:** NEW shared admin lib `admin/src/lib/kiosk/` (Option 3 generalized hooks for cross-feature reuse — F-013 retrofit candidate TD-F015-01); NEW SSE pattern formalized in race-result module via `@Sse()` decorator + RxJS Subject (reuses F-005 pattern); NEW `check_in_logs` MongoDB collection (audit trail; PII boundary ObjectId only); NEW Redis keys `checkin:lock:{raceId}:{bib}` SETNX 5s TTL distributed lock + `checkin:race:{raceId}:stats` 60s aggregate cache; chip-verification module boundary preserved (F-015 zero imports verified by BR-CK-20). **Conventions impact:** 5 NEW patterns minted — (1) Shared admin lib pattern at `admin/src/lib/` (second consumer after F-014 deriveAthleteStatus.ts); (2) Multi-station SSE realtime sync via `@Sse()` + RxJS Subject; (3) Redis SETNX distributed lock + MongoDB atomic update (two-tier guard); (4) CMND PII boundary (last-4 visual match, never store); (5) F-013 hook extraction Option 3 generalized (drop feature-specific prefix). Reused: F-013 BR-AF-23 verbatim port mandate (logic equivalence preserved per Manager Plan Option 3 approval), F-013 runtime guard pattern, F-013 multi-input + tap target standards, F-013 idle reset + Web Audio sound, F-013 fullscreen API user-gesture trigger, F-014 9-status enum reuse pattern, F-014 dirty form/save indicator pattern. **DB / Cache impact:** MongoDB NEW collection `check_in_logs` + race schema add `checkInWindow` field (sparse, default null until migration); MySQL NO change; Redis 2 NEW keys; AWS S3 NO change. **9 TD added** (TD-F015-01..09) per known-issues.md. Branch `5bib_racemonitor_v1`, KHÔNG merge main per cluster policy. NO git commit yet (Danny option C "commit hết một cục" pending — bulk commit later separate action). **Lessons learned:** (1) Coder agent crash mid-task recovery pattern proved viable — finisher Coder picked up where prior Coder stopped (50% files), wrote remaining 30% (tests + DTOs + migration + RaceTabsNav) + 03 doc; lesson: when API errors interrupt agent, file inventory + resume mode work, don't restart from scratch; (2) Worktree path discipline matters — F-015 Coder initially confused main-repo (`release/v1.6.4`) vs worktree (`5bib_racemonitor_v1`); lesson: every Coder/QC prompt MUST include explicit worktree path + `cd` instructions to prevent branch confusion; (3) F-013 hook extraction Option 3 (generalized shared lib) succeeded over Option 1 (path-import) — name change required (`useKioskFullscreen` → `useFullscreen`) but logic equivalence preserved, future cross-feature kiosk patterns benefit, F-013 retrofit deferred (TD) acceptable trade-off; (4) TD-F005-01 placeholder finally RESOLVED — `racekit_received` field actually written by F-015; pattern lesson: always track placeholder TDs to closure feature, Cluster #4 placeholder lived ~3 weeks before resolution; (5) chip-verification boundary discipline worked — explicit BR-CK-20 grep audit + Coder + QC both verified zero matches; lesson: when 2 modules cover overlapping problem domain, explicit boundary BR + grep audit prevents accidental coupling; (6) Multi-station race condition design pattern (Redis SETNX + MongoDB atomic findOneAndUpdate two-tier guard) reusable for any feature where multiple admin clients mutate same resource — future candidates Volunteer Hub assignment conflicts, Medical Incident dispatch; (7) Race Ops 10-tab precedent set (was 9 since F-008v2); tab overflow-later policy (option A) accepted; refactor "More" dropdown when shell exceeds 12 tabs. |
| 2026-05-08 | FEATURE-014 | Athletes tab + Settings full redesign (Race Ops Cluster #8 #2) | NEW_MODULE+REFACTOR | 50 admin frontend files: athletes/ (NEW orchestrator page.tsx replacing F-007 placeholder + 14 components AthletesTabBody/AthletesFilterBar/AthletesTable/AthleteRow/BulkActionBar/AthleteEditDrawer/AthleteProfileDrawer/AthletesEmptyState/StatusBadge/ChangeStatusDialog/AuditLogTimeline + 5 hooks useAthletesList/useAthleteFilters/useAthletesBulkActions/useAthletesExport/useAthletesSearch + 4 modules athletes.constant/athletes.microcopy/athletes.types/lib/deriveAthleteStatus + 8 specs 1 executable + 7 deferred) + settings/ (REWRITE page.tsx 1692→268 LOC composer + SettingsLayout + 6 sections RaceMeta/Course/Timing/Publishing/Integrations/Advanced with sub-components LifecycleStepper/OverrideStatusDialog/CourseTable/SponsorDialog/BrandingForm/SponsorsTable + 2 hooks useDirtyFormPerSection/useUrlHashScroll + section-shared.types + 2 deferred specs); 5 PRESERVED stack components ZERO diff (F-010 TimingDetectionConfigSection + F-012 trio + timing-presets.constant) | 🔴 HIGH | **Race Ops Cluster #8 feature #2 SHIPPED.** Athletes tab NEW_MODULE replaces F-007 placeholder; Settings 1692-LOC legacy editor REFACTORED to 268-LOC composer + 6 sections + sticky-nav layout. **Option C client-derive locked** (Danny APPROVED) — ZERO backend modify, ZERO schema migration, ZERO SDK regen, ZERO new Redis key. NEW `admin/src/lib/deriveAthleteStatus.ts` (admin shared lib root established) derives Race Ops Expert 9-status enum REG/PICKED/DNS/LIVE/FIN/DNF/CUT/DSQ/MED purely from existing race-result fields (`dnf` / `dnsChipFail` / `chipTime` / `OverallRanks` / `timingPoint` / `editHistory[]`). Manual override via `editHistory[]` field=`status` ALWAYS WINS (BR-AS-02 trust-admin philosophy) — prevents auto-undo when admin DSQs an athlete who later pings a chip. MED + CUT have NO vendor signal — only manual override per Race Ops Expert advisory §2 (BTC race-day judgment). DSQ derives from 3 paths (editHistory + timingPoint sentinel "DSQ-CP3" + dsqReason text); DNF from 3 paths (numeric `dnf>0` + boolean `dnf===true` + timingPoint='DNF'); FIN gates on `hasFinishMarker AND hasTimeData AND hasFiniteRank` rejecting vendor sentinels (`'-'`/`'00:00:00'`/`'0'`); DNS gated on `raceStatus === 'ended'` preventing pre-race no-show mis-classification; LIVE recognizes partial split via timingPoint!=='FINISH'/'DNS'/'DNF'; PICKED via `racekitReceived` (camelCase + snake_case both tolerated); REG fallback default never returns null. **20/20 deriveAthleteStatus.spec.ts PASS** executed via `npx jest --config jest.kiosk.config.cjs` (most security-critical correctness rule of cluster — BR-AS-01). Settings refactor uses **section composer pattern** — `page.tsx` is thin shell orchestrating common state (`race`/`editForm`); each of 6 sections is self-contained owning its own form state + save mutation via react-hook-form. **SettingsLayout** provides sticky left rail (desktop) / horizontal scroll (mobile <1024px) + active section highlight via IntersectionObserver in `useUrlHashScroll` + chấm cam (orange dot) per nav item driven by `useDirtyFormPerSection.dirtyMap` (BR-AS-25..28). No autosave block, no leave-confirm — admin trust philosophy. URL preserved via hash anchor `#section-id` enabling bookmark + section discovery; reduced-motion respected. **6-section IA established**: Race Meta / Course / Timing / Publishing / Integrations / Advanced — Formula & Fees DROPPED per audit empty (BR-AS-54). `cacheTtlSeconds` MOVED to Integrations per BR-AS-39. **Drawer pattern** for Athletes — shadcn `Sheet` right-side 480px desktop / fullscreen mobile + drawer state machine `mode: 'edit'\|'profile'\|'closed'` prevents two drawers stacking. Two-tab toggle inside drawer (`edit | profile`) — `AthleteProfileDrawer.tsx` is thin wrapper opening merged drawer in `mode='profile'`. **F-011 status-aware guard pattern** reused on both tabs — Athletes: `raceStatus === 'draft'` → `<AthletesEmptyState variant='draft-guard'>` deep-link to `settings#race-meta` (filter bar mounted but no API fetch attempted; zero-data short-circuit); Settings: LifecycleStepper forward-only + race.status guards in Timing/Publishing. **9th successful BR-AF-23 verbatim port through cluster** — 64/64 logical fields + 7/7 stack pieces preserved byte-for-byte (PAUSE-AS-02 field-mapping THE planning artifact saved 6+ ambiguous decisions); 5 preserved components ZERO diff verified via `git diff --stat HEAD -- "settings/components/"` (TimingDetectionConfigSection 349 LOC + TimingFormulaTooltipContent 132 + TimingPresetComparisonTable 191 + TimingPresetRationalePanel 107 + timing-presets.constant 56 — F-008v2 + F-010 + F-012 stack untouched; CourseMapFullpageLinkCard F-009 untouched). **CSV export header + BOM + 3 F-014 columns appended** preserved verbatim (`Rank/BIB/Name/Gender/Category/ChipTime/GunTime/Pace/Gap/Nationality` + BOM `﻿`). **Conditional reveals byte-for-byte preserved** (`pixEventUrl` shown iff `enable5pix`; `privateListLimit` shown iff `enablePrivateList` — BR-AS-47). **Override reason validation** ≥10 chars enforced via `reason.trim().length >= 10` for DSQ/DNF/CUT/MED only (BR-AS-48); PICKED/REG/LIVE/FIN/DNS allow empty reason. **Lifecycle stepper forward-only ORDER preserved verbatim** with `ended` terminal lock (BR-AS-36). **Sponsor dialog Select cleanup** normalized legacy non-standard `<Select items=…>` to `<SelectContent><SelectItem>` children (same enum semantics). **Description normalized** legacy raw `<textarea>` to shadcn `<Textarea>` per BR-AS-43 (BR-AF-23 explicit exception). **Audit log surface** via `AuditLogTimeline.tsx` reads `editHistory[]` last 5 entries chronologically reverse (no audit-log module needed; reuses existing subdoc per Manager Option A). **4 backend gates resolved per Danny LOCKED Option C frontend-only**: Gate 1 bulk endpoint MISSING → Manager Option B placeholder (disabled buttons + tooltip "Endpoint chưa sẵn sàng — F-014.5", BR-AS-18 satisfied); Gate 2 status field MISSING → client-derive 9-status; Gate 3 search endpoint EXISTS at `race-result.controller.ts:99` (diacritics-folding NOT verified at fixture level — TD-F014-04); Gate 4 audit-log module MISSING → reuse `editHistory[]` subdoc. **Selection cap=500 enforced** via `BULK_ACTION_CAP=500` + `capExceeded` flag inline banner (BR-AS-17). **Search debounce 300ms + flush** exposed for Enter-key shortcuts (BR-AS-09). **Status priority sort** static comparator `LIVE → DNF → MED → CUT → DSQ → FIN → PICKED → REG → DNS` + numeric BIB tie-breaker (BR-AS-07). **Server-side pagination 50/page + load-more** + URL-synced filter state via `useSearchParams` (BR-AS-06/08/10/11). **54/54 BR-AS rules covered** (52 static-verified + 2 partial via TD-F014-01 placeholder). **Zero TypeScript errors** in F-014 paths. **Zero security gaps** — no new IDOR surface, bulk placeholder cannot trigger backend (verified via grep no `mutateAsync`/`useMutation`/`fetch`/`adminController*`/`raceResultController*`), audit log read-only, no PII in URL params, reason `<Textarea>` maxLength={500} React-text auto-escaped no `dangerouslySetInnerHTML`. **Test execution**: 1 spec executed PASS (deriveAthleteStatus 20/20); 9 deferred specs (53 cases) static-reviewed sound + 1 BR-AF-23 audit programmatic spec (11 it() blocks) — activation = 1-line `testRegex` flip when admin RTL stack lands (TD-F013-TESTSTACK linked). **PRD UI states verified**: Loading/Empty zero-data/Empty zero-match/Data/Error/Success/Dirty/Draft-race-guard implemented; Submitting (bulk) placeholder; Offline deferred TD-F014-07. **Architecture impact:** first feature using `admin/src/lib/` shared lib root (single file `deriveAthleteStatus.ts` currently); section composer pattern proven for >40-field admin pages; 6-section IA reusable for future settings/admin pages; client-derive status alternative pattern (Option C) viable without schema migration — saves 1 cluster of work; acceptable trade-off (status logic duplicate F-013 5-status / F-014 9-status, refactor when schema ready TD-F014-02). **Conventions impact:** 5 NEW patterns minted — (1) Client-derive status with editHistory precedence; (2) Sectioned-scroll IA with sticky left nav + hash deep-link + IntersectionObserver active highlight + reduced-motion respect; (3) Per-section save state with chấm cam dirty indicator; (4) Side drawer for edit + profile (preserves list context — alternative to modal/inline); (5) Server-side pagination with URL-synced filters (debounce 300ms + flush). Reused: F-013 runtime guard pattern, F-013 BR-AF-23 verbatim port mandate (9th cluster success), F-011 status-aware guard, F-012 shared-constant module pattern. **DB / Cache / S3 impact: ZERO** (Option C frontend-only — defer status field migration to FEATURE-016+). **Tech debt new: 8 items** — TD-F014-01 (medium, bulk action UI placeholder until F-014.5 backend ships), TD-F014-02 (low, status derivation duplication F-013/F-014 → refactor when schema ready), TD-F014-03 (low, 9-10 deferred specs awaiting RTL stack TD-F013-TESTSTACK linked), TD-F014-04 (medium, search diacritics-folding unverified — must run real-world VN query "Nguyen"→"Nguyễn" against staging 2K-athlete fixture before deploy claims green), TD-F014-05 (low, page.tsx 268 LOC vs 200 target acceptable cosmetic — verbatim editForm seeding per BR-AF-23 mandate), TD-F014-06 (low, contact action stub Mailchimp/SMS deferred Phase 2), TD-F014-07 (low, offline banner not implemented — defer F-015 rich profile cluster), TD-F014-08 (medium, Excel `.xlsx` per BR-AS-19 currently CSV-only — Coder hook outputs CSV blob; Excel deferred F-014.5 OR BA re-LOCK CSV-only). Branch `5bib_racemonitor_v1`, KHÔNG merge main per cluster policy. NO git commit yet (Danny option C "commit hết một cục" pending — bulk commit later separate action). **Lessons learned:** (1) Option C client-derive pattern proven viable for status enum without schema migration — saves 1 cluster of work; trade-off (status logic duplicate F-013/F-014, refactor when schema ready TD-F014-02) acceptable; (2) Manager pre-flight FALSE FLAG on tab count — BA initial claim "actual = 8" was wrong (actual = 9 per F-008v2). Lesson: verify with `git show HEAD:path` + actual array count, not memory grep; (3) F-013 working-tree-only state caused confusion — Coder + QC needed to verify F-013 paths exist in working tree (yes) before importing. Lesson: when worktree pattern in play, document branch state explicitly in init; (4) 1692 LOC settings refactor doable in 1 cluster feature when (a) BR-AF-23 audit checklist is THE planning artifact (PAUSE-AS-02 field map saved 6+ ambiguous decisions), (b) preserve untouched components verbatim (5 components ZERO diff), (c) client-derive avoids backend rework; (5) BR-AF-23 audit programmatic spec + spot-check is reliable — Manager + QC both verified 64/64 fields + 7/7 stack pieces present without missing; (6) Manager Option B placeholder pattern (disabled UI + tooltip naming next-feature gate) reusable for any "backend gap blocks UI feature" scenario — UI ships, BE follows in dot-release. |
| 2026-05-08 | FEATURE-013 | Result Kiosk standalone (BTC tent BIB lookup) (Race Ops Cluster #8 — first feature) | NEW_MODULE+EXTEND_EXISTING | 21 admin files: 2 MODIFY (`result-kiosk/page.tsx` REPLACE F-007 placeholder + `globals.css` +8 LOC `body[data-fullscreen="true"]` containment) + 8 NEW components (KioskModeProvider/BibNumberPad/KioskResultCard/KioskIdleOverlay/KioskExitButton/KioskTabBody/KioskBibInputScreen/KioskResultScreen) + 4 NEW hooks (useKioskFullscreen/useKioskIdle/useKioskSound/useResultLookup) + 3 NEW root modules (kiosk.constant/kiosk.microcopy/kiosk.types) + 6 NEW specs (`__tests__/`) + 1 helper jest config (NOT in scope-lock count) | 🟢 LOW | **Race Ops Cluster #8 OPENS with 1 feature shipped.** Standalone full-page kiosk tab body for BTC tent BIB lookup at race day; replaces F-007 placeholder. Web Audio + native Fullscreen API user-gesture co-location pattern (both APIs satisfied at single "Bật chế độ Kiosk" CTA click handler). Runtime guard `isAthleteDetailResponse(x): x is AthleteDetailEnvelope` for SDK unknown response (BR-RK-11). 8th successful BR-AF-23 verbatim port through cluster (`KioskResultCard.parseSplitsFromData` byte-for-byte from `frontend/app/(main)/races/[slug]/[bib]/page.tsx::178-256`, single-import-fix drift = drop unused `CheckpointConfig` arg + `services` field). Context Provider + 4 hooks separation (state machine `mode\|bib\|result\|loading\|soundEnabled` decoupled from DOM/Web Audio/timer/SDK side-effects → independently testable). 5 statuses rendered: FIN/DNS/DNF/DSQ/LIVE via `deriveKioskStatus` + status-aware UI (BR-RK-03 DNS hides chip/gun times, BR-RK-04 DNF shows last CP, BR-RK-05 DSQ public reason ONLY HTML-stripped via regex, BR-RK-08 LIVE partial splits + "Đang trên đường"). 60s idle auto-reset + last-10s countdown overlay (BR-RK-06). Web Audio beep success 800Hz/100ms + error 300Hz×2/200ms gap, localStorage `5bib:kiosk-sound` persist (BR-RK-10). DSQ privacy = double defense: server strips `_id`/`editHistory`/`isManuallyEdited` (controller line 148) + client KioskResultCard reads only public allowlist `bib/name/distance/dsqReason` (HTML-stripped via `/<[^>]*>/g`). Touchscreen UX: ≥60×60px controls, ≥80×80px digit pad, `touchAction:manipulation` instant tap feedback. Brand audit PASS: 4 files use `#FF0E65`, ZERO `#1D49FF` legacy. **20/20 tests PASS** (`kiosk.types.spec.ts` BR-RK-11 runtime guard 13 cases + `deriveKioskStatus` 7 cases — most security-critical rule executed); 5 specs (33 tests) static-sound deferred via Manager STOP #5 NO npm install — admin lacks `@testing-library/react` + `jest-environment-jsdom` + `@types/jest` + `ts-jest`; 1-line `testRegex` flip in `jest.kiosk.config.cjs` activates all 33 when admin gains test stack (TD-F013-TESTSTACK). 11/11 BR-RK rules covered (1 executed, 5 spec-sound, 4 source-review-only). +8 LOC `globals.css` append ≤20 LOC mandate scoped strictly to `body[data-fullscreen="true"]` selector (overflow:hidden + height:100vh containment for kiosk touchscreen layout). NEW reusable fullscreen primitive — F-011 race-ops shell will adopt later. **Architecture impact:** first feature using `body[data-fullscreen="true"]` CSS primitive in admin shell (future kiosk-style/fullscreen features adopt instead of reinventing); first scope-local microcopy module pattern (`kiosk.microcopy.ts` — alternative to non-existent shared `vn-microcopy.ts`); first SDK unknown-response runtime guard pattern (when generated SDK function returns `unknown`). **Conventions impact:** 3 NEW patterns minted — (1) scope-local microcopy module for features with rich VN strings; (2) SDK unknown-response runtime guard with `isXxxResponse(x): x is XxxResponse` shape validator + JSON.parse limited to vendor strings + React JSX auto-escape; (3) Web Audio + Fullscreen activation co-location at single user-gesture trigger button. Reused: BR-AF-23 verbatim port mandate (8th successful), F-012 shared-constant module pattern. **DB/Cache impact: ZERO** (consume existing `getAthleteDetail` endpoint, reuses existing `master:athlete:*` cache via F-005-era; no new endpoint, no DTO modify, no SDK regen, no new Redis key, no schema migration). **Tech debt new:** 8 items — TD-F013-TESTSTACK (medium, admin RTL stack absent — 1-line flip when added), TD-F013-RL (low, BIB enumeration rate limit absent on `getAthleteDetail` — pre-existing F-005-era), TD-F013-SUBMITHEIGHT (trivial, submit button 60px not PRD-aspirational 80px), TD-F013-IDLE-WIRING (low, 60s auto-reset not unit-tested — bundle with TESTSTACK), TD-F013-TABLET-UAT (medium MUST-DO, real iPad/Android UAT before race-day flip-on), TD-F013-CERT-PRINT (Phase 2, PAUSE-RK-04 deferred), TD-F013-EN-LANG (Phase 2, PAUSE-RK-09 deferred), TD-F013-MULTI-BIB (Phase 2, PAUSE-RK-06 deferred). Branch `5bib_racemonitor_v1`, KHÔNG merge main per cluster policy. **Lessons learned:** (1) Manager pre-flight branch check critical — F-011 fullscreen claim was phantom (not on branch as PRD R1 claimed) → caught at /5bib-plan, BA patched R2; protocol: ALWAYS `git ls-tree` target branch before approving "REUSE F-XXX pattern" claims in PRD; (2) BA assuming SDK function name → wrong (`raceResultControllerFindAthleteByBib` vs actual `raceResultControllerGetAthleteDetail`); PRD must cite SDK function from actual `sdk.gen.ts` line number, not memory; (3) "NO npm install" Manager STOP trigger forces creative solution (defer 5 specs to TD instead of breaking scope) — judgment call accepted by QC; trade-off scope discipline vs test coverage acceptable when 1 critical spec (BR-RK-11) executes + 5 logical reviews pass; (4) 8th successful BR-AF-23 verbatim port through cluster — pattern is now battle-tested; single import drift (drop unused `CheckpointConfig`) is the only acceptable delta. |
| 2026-05-07 | FEATURE-012 | TimingDetectionConfigSection UX Hints & Formula Explanation (Race Ops Cluster #7 — hints CLOSED) | EXTEND_EXISTING (presentation layer only — UI hints/explanation surfaces ABOVE F-010 form, ZERO backend modify, ZERO schema modify, ZERO API contract change) | 5 admin frontend files Round 2: 1 NEW shared module (`timing-presets.constant.ts` 56 LOC single source of truth `TIMING_PRESETS: Record<CourseType, TimingPreset>` + `PRESET_LABELS_VI` + `CourseType` literal union) + 3 NEW components (`TimingFormulaTooltipContent.tsx` 132 LOC Surface 1 — 4 inline tooltips with custom click-to-toggle popover + 3-layer content formula `<code>` + VN explanation + 1 example + a11y aria-expanded + Escape + outside-click; `TimingPresetComparisonTable.tsx` 191 LOC Surface 2 — 4 rows × 3 cols comparison table + 2 footer rows + current preset column highlight, imports values from shared `timing-presets.constant.ts`; `TimingPresetRationalePanel.tsx` 107 LOC Surface 3 — "Tại sao preset này?" expandable panel rendering ALL 3 paragraphs ROAD/TRAIL/ULTRA when open, current preset highlighted via `border-l-4 border-amber-500 bg-amber-50/70` + `(đang chọn)` annotation, trigger always visible no `null` short-circuit, citation footer verbatim PRD) + 1 MODIFY (`TimingDetectionConfigSection.tsx` 363 → 349 LOC -14 net DEDUPED inline `TIMING_PRESETS` constants → import from shared module + wired 4 tooltip triggers + comparison table block + rationale panel block; F-010 form behavior preserved BYTE-FOR-BYTE: `applyPreset/updateField/handleSave/validateRange/save mutation` UNCHANGED, call site `:120 TIMING_PRESETS[preset]` byte-identical only source moved to shared module) | 🟢 LOW | **Race Ops Cluster #7 hints CLOSED.** Post-F-010 deploy + BTC UAT screenshot feedback "BTC khó hiểu vì sao Trail paceBuffer 1.35 vs Road 1.10" → init hints feature small-scope ship trong 1 sprint. **3 hint surfaces shipped:** (1) 4 inline tooltips next to each timing config field (BR-FH-01 paceBuffer formula `pace_threshold = expected_pace × pace_buffer` + BR-FH-02 paceAlertThreshold formula `pace_drop_ratio = current_split_pace / previous_split_pace; flag IF ratio < threshold` + BR-FH-03 overdueMinutes formula 3-derivation `expected_arrival → elapsed_since_expected → flag PHANTOM` + BR-FH-04 confidence multiplier formula + ROAD 0.20/1000 athletes example per task spec); (2) Toggleable preset comparison table 4 rows × 3 cols + 2 footer rows + current preset accent ring; (3) "Tại sao preset này?" rationale panel with ALL 3 paragraphs always rendered + current highlighted + citation footer "Nguồn: Race Ops Expert + Sports Domain Expert F-010 advisory (2026-05-07)" verbatim PRD. **QC Round 1 REJECTED** với 2 P0 BLOCKERs: BLOCKER #1 Surface 3 ships 1 paragraph instead of 3 (BR-FH-07 spec drift, UAT-FH-04 fail) + BLOCKER #2 Comparison table values contradict form's `TIMING_PRESETS` constants (TRAIL overdue=60 in table vs 45 in form, ULTRA overdue=120 vs 60, confidence multipliers also drift — race-day BTC misinformation risk). **QC Round 2 APPROVED** với root-cause fixes: BLOCKER #1 fix rewrote `TimingPresetRationalePanel.tsx` 63→107 LOC 3 paragraphs always + current highlight + trigger always visible; BLOCKER #2 fix created NEW shared `timing-presets.constant.ts` module (single source of truth imported by both form + table — eliminates drift forever, also resolves TD-F012-02 P2 from Round 1). All 4 MINORS resolved: tooltip content verbatim PRD (BR-FH-01..04), removed "(Danny chốt)" data-cell annotation moved to legend footnote, citation footer wording per PRD verbatim, 4th file accepted as shared CONSTANT module not component per PAUSE-FH-02 LOC threshold logic. **10/10 BR-FH verified Round 2** (vs Round 1 6.5/10). **168/168 timing-alert backend regression preserved** (5.684s — F-010 baseline intact; F-012 ZERO backend modify ZERO test add). **TypeScript clean F-012 scope** (`pnpm tsc --noEmit | grep "settings/components/Timing\|timing-presets"` → 0 errors). **F-010 form behavior preserved BYTE-FOR-BYTE** — `applyPreset/updateField/handleSave/validateRange` UNCHANGED, save mutation `timingAlertAdminControllerUpsertConfig` UNCHANGED, 4 `<Input>` IDs/onChange UNCHANGED. **BR-AF-23 settings parent preserved** — F-012 ZERO touch `settings/page.tsx` (mtime 11:20 vs F-012 files 16:07; pre-existing +14 LOC visible in git diff belongs to F-008 v2/F-010 in-flight, NOT F-012). **Click-each preset alignment ROAD/TRAIL/ULTRA verified** across all 4 fields (form ↔ table single source of truth — clicking ROAD fills `1.10/0.80/30/0.20` matches table ROAD col; clicking TRAIL fills `1.35/0.45/60/0.15` matches; clicking ULTRA fills `1.50/0.40/120/0.10` matches). **Architecture impact:** Settings tab additive multi-feature composition continued (3rd link card slot pattern: F-008 v2 SettingsLinkCardsSection + F-009 CourseMapFullpageLinkCard + F-010 TimingDetectionConfigSection + F-012 hints inside that section); custom click-to-toggle popover pattern (no shadcn Tooltip in repo); shared constant module pattern for cross-component data sync. **Conventions impact:** 3 NEW patterns minted — (1) Inline tooltip surface với formula+VN+example pattern + a11y triple (aria-expanded + Escape + outside-click); (2) Multi-paragraph rationale với current selection highlight (render ALL options always, highlight current via accent border-left + bg + annotation); (3) Shared constant module to prevent cross-component data drift (extract shared values to dedicated `*.constant.ts` when same data needed by ≥2 components). **DB/Cache impact: ZERO** (presentation layer only, no schema modify, no Redis key, no API contract change). **Tech debt resolved:** TD-F012-02 (P2) — TIMING_PRESETS extracted to shared module (root-cause fix Round 2). **Tech debt new:** TD-F012-01 (P3 optional) — promote click-to-toggle popover to reusable `<InlineHintPopover />` if reused trong feature kế tiếp. **SDK regen: NOT performed** (DTO unchanged). Branch `5bib_racemonitor_v1`, KHÔNG merge main per cluster policy. Lessons: (1) root-cause fix via shared constant module > spot-fix individual values when same data needs 2 consumers — Race Ops domain failure mode "values mismatch between consumers" caught by QC Round 1; (2) Surface 3 spec drift (1 vs 3 paragraphs) caught by careful Phase 5 PRD compliance check against UAT testable assertions — UAT exact wording catches defeats-the-purpose drifts; (3) custom popover with full a11y (aria + keyboard + click-out) acceptable when shadcn primitive not in repo + NO install policy — future feature reusing pattern → consider promoting `<InlineHintPopover />` shared primitive (TD-F012-01). |
| 2026-05-07 | FEATURE-011 | Race Ops UI Polish Bugfix (Race Ops Cluster #6 — POST-DEPLOY POLISH closed) | BUGFIX (presentation layer only — UI polish + status-aware guard) | 5 admin frontend files: 2 atomic Bug #1 (`(dashboard)/layout.tsx` +6 LOC additive `data-admin-sidebar`/`data-admin-topbar` attrs + `globals.css` +8 LOC `body[data-fullscreen]` extension to hide admin shell); 1 atomic Bugs #2/#3/#4 (`AthleteFlowChart.tsx` +88/-16 LOC: optional `raceStatus?` prop + pre-race guard "Race chưa khởi động — chờ start gun" + ghost track 100% fallback + vertical right-column stacking + expected-label right-pin when expPct<5); 1 wiring (`CommandCenterLayout.tsx` +1 LOC `raceStatus={raceStatus}` prop pass-through additive với F-010 DnsBreakdownCard render block); 1 microcopy (`command-center/page.tsx` +2/-2 LOC PageHero meta ternary "Race-day operations cockpit" → "Race Command Center") | 🟢 LOW | **Race Ops Cluster #6 POST-DEPLOY POLISH closed.** Post-F-009 deploy → BTC UAT trên Giải Công An pre-race state phát hiện 5 polish bugs → init bugfix small-scope ship trong 1 sprint (precedent established: post-deploy UX feedback loop pattern). **5 bugs fixed atomic:** (1) Fullscreen toggle ẩn full chrome (admin sidebar + topbar + race-ops shell header) via dual-layer scope expansion (admin shell `[data-admin-sidebar]` + `[data-admin-topbar]` + race-ops shell `[data-race-ops-shell-header]` all hide via parallel selectors); (2) AthleteFlowChart pre-race state guard — status-aware UI guard pattern minted (race.status `'draft'`/`'pre_race'` → return single neutral grey card "⏱ Race chưa khởi động — chờ start gun"; FlowRows skipped while F-005 backend health() calc preserved verbatim lines 144-145); (3) Ghost dashed bar full-width fallback (`width: expPct > 0 ? '${expPct}%' : '100%'`); (4) Right column vertical flex-col stacking + expected-label right-pin when `expPct<5` to avoid collision; (5) PageHero subtitle "Race Command Center" canonical (live: "RACE LIVE — Race Command Center") — VN microcopy 100% mandate honored, grep `cockpit` → 0 results. **F-010 ADDITIVE coexistence verified** — DnsBreakdownCard import line 49 + render block lines 286-293 preserved verbatim; F-011 owns exactly 1 line (`raceStatus={raceStatus}` prop) at AthleteFlowChart in CommandCenterLayout. Both orderings safe (F-010 lands first OR F-011 lands first — additive diff). **F-005 health() calc preservation** — lines 105-106 byte-for-byte (now lines 144-145 after pre-race guard insertion above). Frontend race-status guard interprets per-state ABOVE backend logic, never bypasses. **BR-AF-23 byte-for-byte preserve mandate honored** cho shared `(dashboard)/layout.tsx` — used by ALL admin routes (NOT just race-ops); F-011 only adds 2 data-attributes additive, ZERO class change, ZERO markup restructure. **F-008 v2 fullscreen pattern PRESERVED + EXTENDED** — existing `[data-race-ops-shell-header]` translateY rule untouched; F-011 adds parallel selector for admin shell with `display:none !important` (justified per F-008 v2 BR-CC2-09 precedent — overrides Tailwind `lg:flex`). **168/168 backend timing-alert tests PASS** (F-010 baseline preserved; F-011 zero backend modify — automatic preserve). **10/10 BR-PB verified, 5/5 UAT-PB self-smoke + QC independent verify, 0 BLOCKERs, 0 forbidden file touches, 0 new TD.** **Architecture impact:** Race Ops 9-tab shell fullscreen scope NOW dual-layer (admin shell + race-ops shell); F-008 v2 mechanism preserved, scope expanded only. AthleteFlowChart pre-race state guard added — race lifecycle state-aware render path. **Conventions impact:** 3 NEW patterns minted — (1) Status-aware UI guard pattern (race.status discriminator → conditional render bypass logic-correct-but-UX-misleading states); (2) Fullscreen scope dual-layer pattern (admin shell + route-specific shell both hide via separate data-attribute selectors, route-agnostic admin layout shared via attrs additive); (3) Post-deploy UX feedback loop pattern (BTC UAT discovery → BUGFIX init → small-scope polish ship in 1 sprint, cluster polish slot acceptable parallel với feature pipeline). **DB/Cache impact: ZERO** (presentation layer only). **Tech debt: NONE new** (TD-F008-01 frontend Vitest harness carryover unchanged). **SDK regen: NOT performed** (DTO unchanged). Branch `5bib_racemonitor_v1`, KHÔNG merge main per cluster policy. Lessons: post-deploy UX feedback loop ship-in-1-sprint precedent reproducible; risk-profile UX hierarchy reaffirmed (lightweight CSS attr extension acceptable cho fullscreen scope expansion); F-010 + F-011 parallel coexistence successful (additive CommandCenterLayout merge proves pattern reusable cho future cluster parallel features); status-aware UI guard pattern reusable cho any race-lifecycle-dependent component (Awards/Result Kiosk/Athletes future). |
| 2026-05-07 | FEATURE-010 | Formula Correction & Config Upgrade — Timing Intelligence (Race Ops Cluster #6) | EXTEND_EXISTING (timing-alert config schema + race-result cross-module DI + dashboard-snapshot DNS breakdown + CUTOFF_RISK alert type) | 24 files: 16 backend (14 MODIFY: 3 schemas + 2 DTOs + 6 services + 1 module + 1 controller + 3 specs; 2 NEW: update-dns-chip-fail.dto + dashboard-snapshot-dns-breakdown.dto) + 8 admin (4 MODIFY: settings page additive section + CommandCenterLayout DnsBreakdownCard render + AlertsListView CUTOFF_RISK case + types.gen.ts manual augment; 3 NEW: TimingDetectionConfigSection + DnsBreakdownCard + DnsChipFailToggle; 1 type-augmentation: lib/timing-alert-api.ts pure type extend) | 🟡 MED | **Race Ops Cluster #6 closed parallel với F-011 in-flight.** **3 expert advisory integration:** Race Operation Expert ✅ APPROVED 8/8 PRD items (CUTOFF_RISK auto-resolve + DNS 3-state + PACE_BUFFER realistic per course_type + isPaceAlert thresholds + overdue thresholds + MIDDLE_GAP escalation + wall-clock overdue + percentage-based confidence) + Sports Domain Expert 🟡 APPROVED with notes (Danny preset adjustments locked: TRAIL paceBuffer 1.25→1.35 mid-range compromise, TRAIL isPaceAlert 0.50→0.45, ULTRA paceBuffer 1.40→1.50 upper bound; ROAD all values preserved PRD original) + Strategic Scout TOP 10 missing race-ops features tracked separately post-F-014 roadmap. **168/168 timing-alert backend tests PASS** (149 baseline + 19 NEW F-010 across miss-detector + projected-rank specs covering CUTOFF_RISK creation/skip/severity, paceBuffer 1.10 vs 1.50 override, OBS-1 wall-clock with lastPollAt, OBS-2 MIDDLE_GAP severity escalation WARNING/HIGH/CRITICAL paths, percentage-based confidence multiplier 0.10/0.20/0.50 + totalRegistered=0 fallback to 50 + cap at 1.0). **18/19 BR-FC verified, 1 deferred to TD-F010-V1-test-gap** (computeDnsBreakdown direct unit test). **0 BLOCKER QC findings.** **Schema migration NONE** — all new fields OPTIONAL with defaults (1.10/0.80/0.20/null course_type), safe for 195 races + 94K race_results. **Cross-module DI proven** — RaceResultModule → TimingAlertConfigModel via `MongooseModule.forFeature()` no circular import (TimingAlertModule does NOT import RaceResultModule). **detection_type enum extend additive** — `'PHANTOM' \| 'MIDDLE_GAP' \| 'CUTOFF_RISK'` (default still PHANTOM, existing alerts preserved). **CUTOFF_RISK auto-resolve dual-trigger** — next CP time OR Finish chiptime in poll service. **Cache impact zero** — no new Redis keys (DNS breakdown computed in cached `master:rr-snapshot:` 15s TTL naturally), no migration. **F-005 forbidden services formally UNLOCKED** per Manager Scope Lock §rows 4-9 (miss-detector + projected-rank + timing-alert-poll service signature changes for CUTOFF_RISK + OBS-1 + OBS-2 + confidence formula + paceBuffer per course_type). **SDK regen verified live swagger** — 4 new fields + dnsBreakdown + dns-chip-fail endpoint REGISTERED, manual types.gen.ts augment matches byte-for-byte. **Branch `5bib_racemonitor_v1`, KHÔNG merge main**. **Lessons learned:** Expert advisory collaboration model F-009 → F-010 reproducible (Race Ops + Sports Domain + Strategic Scout precedent established for cluster features); Danny preset compromise pattern (lower bound TRAIL 1.35 vs Sports recommended 1.40-1.50 max conservative — field-test mandate logged TD-F010-V1-tuning); cross-module Mongoose DI proven safe; field-test post-deploy mandate next VN trail/ultra race Q2/Q3 2026. |
| 2026-05-07 | FEATURE-009 | Course Map Standalone Tab (Race Ops Cluster #5) | REFACTOR + EXTEND_EXISTING (F-006 modal → 9-tab shell standalone tab body) | 12 files: 0 backend (F-006 ZERO modify per BR-CM2-21) + 9 NEW frontend (CourseMapLayout orchestrator + CourseDistancePicker pills + CourseMapFullView verbatim port 466 lines + GpxUploadSection + CheckpointConfigGrid + ManualDragModeButton + AimsItraDisclaimerBanner + CourseMapFullpageLinkCard + page.tsx replace placeholder) + 3 MODIFY frontend (CourseDialog deprecation banner additive + Settings link card additive + CourseMapTab kept alive) | 🟡 MED | **Race Ops Expert collaboration** — 56 citations integrated vào BR-CM2 (5 personas including 3 Race Ops-specific: Race Director / Race Marshal / Course Designer). Refactor F-006 modal `CourseDialog` (606 lines, 5-tab dialog opened từ Settings → Cự ly) → standalone full-page tab body tại `/races/[id]/course-map` (slot 3 trong 9-tab shell). **7th BR-AF-23 verbatim port SUCCESS** (F-007 settings 1678 + F-008 v2 6 ports + F-009 1 port) — `CourseMapTabInner.tsx` 466 lines port với 23 lines diff / 3 hunks ALL docblock + interface name + function name (ZERO logic drift verified by QC). Multi-distance navigation = course pills (5K/10K/21K/42K) + 4-state status badge (✅ ⚠ ❌ 🔴) + URL query param `?course=courseId` deep-link friendly. AIMS/ITRA disclaimer banner top page với 7-day localStorage dismiss persist (Race Ops Expert mandate). Drag UX divergence từ F-008 v2: lightweight 3s toast (drag = reversible) + MEDIUM-weight confirm modal cho auto-snap (override warning); KHÔNG default 2-step typing modal (Reset destructive only). Distance field READ-ONLY trong checkpoint grid (preserve F-008 v2 Health Matrix dependency on `cp.distanceKm`). F-006 modal preserved 30-day deprecation window (CourseDialog alive trong Settings tab + deprecation banner top + Settings link card additive ABOVE existing trigger line 1145). F-006 backend ZERO modify (4 endpoints + DTOs + cache namespace `master:course-map:` + S3 prefix `courses/` UNCHANGED). F-006 lib reuse 100% (course-map-hooks + course-map-api + ElevationChart + SnapToPolyline.helper). F-008 v2 wrapper reuse: `CheckpointDiscoveryDialogWrapper` direct path import (closes TD-F008-V2-01 — course selector pre-fill từ `?course=` query param). **53/53 F-006 backend tests PASS** + F-008 v2 sample 5/5 PASS = 216 backend regression baseline preserved. **32/32 BR-CM2 verified** (zero NOT_COVERED, 2 TD-deferred TD-F009-01/02). **3 fidelity scores: Architectural=100% gate / Component ≥95% / Workflow ≥95% UPGRADED**. NO partial unlock shell needed (F-007/F-008 v2 territory unchanged). 9-tab shell consume only. Sovereignty safeguard preserved (`setMaxBounds()` lock to race bounds + 5% padding). escapeHtml() XSS guard preserved trong verbatim port. F-005 sub-page parallel preserve 30-day window sync (2026-06-07 hard-delete timing). Branch `5bib_racemonitor_v1`, KHÔNG merge main. Lessons: lightweight toast vs MEDIUM modal vs 2-step typing risk-profile hierarchy established, multi-resource pill picker với 4-state status badge pattern, AIMS/ITRA compliance disclaimer pattern. |
| 2026-05-07 | FEATURE-008 v2 | Command Center Refactor + Full F-005 Migration (Race Ops Cluster #4 RESCOPE) | REFACTOR + EXTEND_EXISTING (full F-005 sub-page → 9-tab shell migration) | 27 files: 3 backend (additive `lastPollAt` DTO + service compute + 2 NEW tests) + 11 NEW frontend (AlertsListView verbatim port + 2 dialog wrappers + Fullscreen + Sound + ResetConfirmModal + RaceStatusPill + SettingsLinkCardsSection + awards/page.tsx + 2 lib hooks) + 10 MODIFY frontend (page + Layout + TopBar + AlertFeedPanel drill-in + settings additive + 3 deprecation banners + lib SDK sync + globals.css fullscreen) + 3 partial unlock shell (RaceTabsNav 8→9, RaceOpsHeader data-attr, middleware 2 redirect) | 🔴 HIGH | **v2 RESCOPE** từ "Canvas 03 fidelity polish" → "Full F-005 migration + 9-tab shell". v1 archived `_archive/v1-canvas-fidelity/` (5 files QC PASS but not deployed; v1 code in-tree, v2 builds upon). 6 F-005 features migrated VERBATIM (BR-AF-23 single-import-fix): AlertsListView 491 lines + Podium/Awards 146 + Sound 880Hz + Reset 2-step typing + AlertDetailDialog 535 + CheckpointDiscoveryDialog 321. 3 NEW: Fullscreen CSS `body[data-fullscreen]` + RaceStatusPill inline body + SSE realtime hook body-scoped (debounce 1500ms invalidate). 9-tab shell expansion (Awards slot 6 between Result Kiosk + Athletes, `enabledIn:["live","ended"]`). Drill-in via `?view=alerts` query param (NOT separate route). Reset 4-layer defense (LogtoAdminGuard + confirmToken=race.slug + status throw + 2-step typing modal). **150/150 backend tests PASS** (148 baseline + 2 NEW `computeLastPollAt`). **42/42 BR-CC2 verified** (zero NOT_COVERED, zero TD-deferred). **3 fidelity scores: Architectural=100% gate / Component ≥95% / Workflow ≥95% UPGRADED** từ v1 ≥90%. F-007 PARTIAL UNLOCK pre-approved 3 files. F-005 sub-page parallel preserve 30-day window (3 deprecation banners + middleware 301 redirects). Branch `5bib_racemonitor_v1`, KHÔNG merge main. Lessons: B3 hybrid drill-in pattern, verbatim port BR-AF-23 chính thức, SSE body-scoped hook robust cleanup, fullscreen CSS attr (KHÔNG F11), 2-step typing modal race-day safety. |
| 2026-05-06 | FEATURE-007 | Race Ops Architectural Foundation (Race Ops Cluster #3) | REFACTOR (race detail page architecture) | 22 files: 16 NEW (1 layout + 8 page.tsx + 6 shell components + 1 middleware) + 7 MODIFY (page.tsx rewrite + globals.css × 2 + 5 hex-swap + 1 backend test fix) | 🔴 HIGH | **v2 RESCOPE** từ "UX Polish" → "Architectural Foundation". v1 archived `_archive/v1-ux-polish/` (10 items polish layer SAI architecture). Build 8-tab race-ops shell foundation cho cluster F-008..F-014. Brand magenta `#FF0E65` migration 19 hex literals. F-005 sub-page 301 redirect 30-day window. F-006 modal parallel preserve. Settings tab 1678-line legacy editor moved VERBATIM (BR-AF-23 byte-for-byte). RaceLiveTimer 4 states + 14 QC adversarial tests PASS. **3 fidelity scores: Component ≥95% / Workflow ≥90% / Architectural 100%**. Race deadline 17/05 Cát Tiên acceptable trượt — Foundation > deadline. Branch `5bib_racemonitor_v1`. Lessons learned: design canvas gate MANDATORY, architectural shape P0 trong PRD section 1, 3 fidelity scores post-implementation. |
| 2026-05-06 | FEATURE-006 | Course Map Visualization (Race Ops Cluster #2) | EXTEND_EXISTING (RacesModule + admin Course dialog + frontend public race detail) | ~28 files: 13 backend (4 new DTO + 1 service + 1 spec + 7 modify + jest infra fix) + 12 admin (5 components + 2 lib + 3 SDK + 1 e2e + fixtures + escapeHtml inline) + 5 frontend (3 components + escapeHtml inline + SDK + race detail integration) + 1 docs | 🟡 MED | GPX/KML upload + parse server-side via @tmcw/togeojson + @turf/turf, Douglas-Peucker simplify (50K→≤5K points), fuzzy match waypoint↔checkpoint 3 levels strict (BR-CM-04 false-positive guard), Leaflet read-only public + admin manual drag mode, pure SVG ElevationChart shared. **66 new F-006 tests PASS** (23 service + 18 QC adversarial + 11 controller + 4 service cache invalidate + 10 DTO/extra) + 135 timing-alert regression maintained. Cache namespace `master:course-map:` + 2-layer SETNX anti-stampede. S3 prefix `courses/` (no expiration). DTO sync MANDATORY honored (quirk hotfix `804f707`). 4 rounds: initial → QC reject XSS BLOCKER + MEDIUM cache invalidation gap → Coder rework escapeHtml both files + removeCourse `redis.del` → QC re-run targeted 4 items APPROVED. F-005 lessons all applied (BR-CC-10, master: namespace, design fidelity, dedicated entity query, pure CSS chart). Branch `5bib_racemonitor_v1`, KHÔNG merge main. |
| 2026-05-05 | FEATURE-005 | Race Day Command Center (Race Ops Cluster #1) | EXTEND_EXISTING (timing-alert + dashboard-snapshot) | ~22 files across 3 commits: 9 backend (5 modify + 4 create) + 13 frontend (6 modify + 6 create + 1 e2e) | 🟡 MED | Extend F-002 dashboard-snapshot với liveLeaderboard + summary cards + Force Refresh endpoint + AlertFeedPanel dedicated. **135/135 tests PASS** (109 baseline + 12 new + 14 QC). 10/10 BR-CC verified. **3 commits:** `1dab534` initial+rework BR-CC-10 → `1b81a2d` visual polish (dedup 3 sections + design tokens + dual-bar overlay) → `c63ee8a` AlertFeedPanel switch data source `recentActivity` → `listTimingAlerts(status:OPEN)`. Design fidelity ~98% match canvas Artboard 3 (race-ops-command.jsx). Tab "Cockpit" → "Command Center" rename UI only (route + query key giữ). Cache namespace `master:rr-snapshot:` migrate từ F-002 cũ. Branch `5bib_racemonitor_v1`, KHÔNG merge main. |
| 2026-05-05 | FEATURE-004 | Reconciliation Download via Backend (fix S3 direct link) | BUGFIX | 3 file (2 FE + 1 spec test) | 🟢 LOW | Diệt bug Danny prod: download S3 URL fail 403 với Bearer Logto. Fix: drop `data.xlsx_url \|\|` short-circuit, luôn dùng backend `/api/reconciliations/:id/download/{xlsx\|docx}`. Bucket S3 giữ private. JSDoc cảnh báo field internal-use only. Playwright spec 11 tests. **MERGED main `4528a02` 2026-05-05 → CI auto-deploy DEV → Danny UAT smoke test pass (TC-DOWNLOAD-08 + SEC-01 verified).** PROD manual deploy pending. |
| 2026-05-05 | FEATURE-003 | Reconciliation Period Bugs + Multi-Month Range Support | BUGFIX + EXTEND_EXISTING | 22 file (12 backend + 5 admin + 5 test) | 🔴 HIGH | Diệt 2 bug Danny report (modal off-by-one, range bỏ tháng cuối) + đổi business model: 1 recon = 1 race × N (1≤N≤12) tròn tháng. **84/84 tests PASS**. 12/12 BR. 2 endpoint mới + compound index 4-field + 2 pattern mới vào conventions. **MERGED main `4528a02` 2026-05-05 → CI auto-deploy DEV → Danny UAT smoke test pass (TD-F003-03 BLOCKER ✅ RESOLVED).** PROD manual deploy pending. |
| 2026-05-04 | FEATURE-002 | Timing Alert UX & Robustness Polish | EXTEND_EXISTING + BUGFIX | 12 files (3 commits: 18607f3+a9969cb+31cc698) + 1 doc + DB backfill 5213 alerts | 🟡 MED | Retroactive 3-round ship. **109/109 regression pass** (was 77/78, +31 new tests F-002, +1 fix pre-existing). Resolved 6 bugs: reset 500 generic, simulator scalar inconsistency post-scenario, alert trajectory empty (contest=null), missing race elapsed clock, generic toast handler cho reset endpoint, empty state message lệch spec. 5 patterns mới vào conventions (exception class > Error, per-severity infinite query, SSE debounce coalesce, re-derive scalars post-mutation, vendor field fallback server-side). Branch `5bib_racemonitor_v1`, không merge main. |
| 2026-05-04 | FEATURE-001 | Timing Alert Discovery + Simulator Completion (Phase A+B+C) | EXTEND_EXISTING | 11 + 1 hotfix (`AddCourseDto.distanceKm`) | 🟡 MED | Pilot race 2026-05-02 đã pass; 51/51 tests; SDK regen + UAT BR-09 còn phải làm trước production. Hotfix `CourseCheckpointDto.distanceKm` 804f707 (related-scope) |

---

## 📝 In-flight features

> Feature đã `/5bib-init` nhưng chưa `/5bib-deploy`.

| ID | Title | Status | Owner | Last update |
|----|-------|--------|-------|-------------|
| _(FEATURE-032 SHIPPED 2026-05-13 — moved to Shipped table)_ | | | | |
| _(FEATURE-030 SHIPPED 2026-05-13 — moved to Shipped table)_ | | | | |
| _(FEATURE-031 SHIPPED 2026-05-13 — moved to Shipped table)_ | | | | |
| _(FEATURE-027 ✅ memory-CLOSED 2026-05-13 — moved to Shipped. Branch `5bib_promo_hub_v1` pending Danny approve push+merge main.)_ | | | | |
| _(FEATURE-025 SHIPPED 2026-05-11 — moved to Shipped table)_ | | | | |
| FEATURE-016 | Reconciliation Include GROUP_BUY + GROUP_BUY_FIXED + CODE_TRANSFER (CRITICAL Financial Bug Fix) | 🟠 v1.6.5 SHIPPED, v1.7.0 PENDING | Danny | 2026-05-08 — **Phase 1 v1.6.5 PATCH ✅ DEPLOYED via release/v1.6.5** (commit `d4b66a3` on main → push `origin/main` + `origin/release/v1.6.5` → CI `deploy-production.yml` auto-deploy PROD). 22/22 NEW tests PASS (18 Coder + 4 QC adversarial), 106/106 reconciliation domain regression. 5/5 BR (v1.6.5 scope) covered. 0 scope creep. 0 security critical/high. 4 file F-016 (3 modify + 1 spec NEW) + 1 QC artifact (preflight spec extension) — package.json/pnpm-lock GIS contamination from race-ops cluster excluded via `git restore` before commit. **TD-F016-FINANCE-01 BLOCKER for accounting flagged in known-issues.** Feature giữ In-flight pending Phase 2 v1.7.0 (recompute service + audit endpoint + admin UI) — start sau Danny UAT PROD pass. **NOT YET** `05-manager-deploy.md` (feature chưa close). |
| _(FEATURE-015 SHIPPED 2026-05-08 — moved to Shipped table)_ | | | | |
| _(FEATURE-009 SHIPPED 2026-05-07 — moved to Shipped table)_ | | | | |
| _(FEATURE-010 SHIPPED 2026-05-07 — moved to Shipped table)_ | | | | |
| _(FEATURE-011 SHIPPED 2026-05-07 — moved to Shipped table)_ | | | | |
| _(FEATURE-012 SHIPPED 2026-05-07 — moved to Shipped table)_ | | | | |
| _(FEATURE-013 SHIPPED 2026-05-08 — moved to Shipped table)_ | | | | |
| _(FEATURE-014 SHIPPED 2026-05-08 — moved to Shipped table)_ | | | | |

Status: `🟡 INITIATED` → `🔵 PRD_DONE` → `🟣 PLAN_APPROVED` → `🟠 CODING` → `🟢 IN_QC` → `✅ DEPLOYED`

---

## 📚 Pre-workflow context (manual PRDs đã có trong repo)

> Đây là PRD/research đã viết tay TRƯỚC khi áp workflow. Không qua gate `/5bib-init` nhưng có thể là input cho feature mới.

### PRDs (chưa init feature)
- `prd-chip-verify-tts-read-name.md` — Chip verification + TTS đọc tên
- `prd-racekit-pickup-report.md` — Race kit pickup report

### Research / Strategy
- `research-5bib-saas-whitelabel-strategy.md`
- `research-5run-po-feature-brief.md`
- `research-5run-ui-design-brief.md`
- `research-rfid-checkin-display-system.md`
- `research-timing-miss-alert-system.md`
- `strategy-5bib-brand-partnership-monetization.md`

### Reports
- `PROGRESS_REPORT.md` (Apr 23)
- `UAT_Report_TeamManagement.md` (Apr 23)
- `RACERESULT_INTEGRATION_SPEC.md` (Apr 23)
- `TODO.md`

> Khi `/5bib-init` cho feature liên quan, Manager nên đọc PRD/research tương ứng để có context — nhưng vẫn tạo `00-manager-init.md` mới theo workflow.

---

## 🔍 Cách dùng

**Khi `/5bib-init`:**
- Đọc bảng "In-flight" → check feature trùng/đụng độ
- Đọc 5 dòng gần nhất ở "Shipped" → hiểu context gần đây
- Đọc danh sách PRD pre-workflow → check feature mới có trùng với PRD đã có không
- Tăng counter → tạo entry mới ở "In-flight"

**Khi `/5bib-deploy`:**
- Xoá entry khỏi "In-flight"
- Append entry mới vào TOP của "Shipped"
- Tăng counter cho lần init tiếp theo
