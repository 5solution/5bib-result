# FEATURE-074: Deploy — ✅ DONE (DEV)
Manager review CLEAN: yoy.util (daysBefore/cumulative correct), service (IDOR BOTH races ✅ + comparable accessible-filtered = no cross-tenant leak + cache300s), controller (YoyCurveQueryDto validate compareRaceId, ticket-scope), FE YoYCard (dropdown + overlay + empty/prompt states). 0 red flag, no-PII.
BE+FE 1 push. 7 util + 168 jest.
Memory: feature-log shipped+counter→075, change-history, known-issues (2 TD).
DEV smoke (Danny): tab Vé → MKT → "So với mùa trước" → chọn giải → 2 đường overlay.
