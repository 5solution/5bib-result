/**
 * F-008 v2 — CheckpointDiscoveryDialog wrapper (verbatim re-export).
 *
 * Per BR-CC2-35 + BR-AF-23 settings precedent: re-export the F-005
 * dialog so command-center components can import via local path.
 */
export { CheckpointDiscoveryDialog } from '../../timing-alerts/components/CheckpointDiscoveryDialog';
