# F-041 Analytics — build-time env (embedded into Next.js client bundle by `pnpm build`).
# Loaded automatically when NODE_ENV=production (default during `pnpm build`).
# `G-PNVB69YRL2` is a PUBLIC GA4 measurement ID (NOT a secret) — safe to commit.
NEXT_PUBLIC_GA_ID=G-PNVB69YRL2
