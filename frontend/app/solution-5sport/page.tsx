/**
 * solution.5sport.vn landing — Server Component per FEATURE-060 BR-20.
 *
 * State + URL-param language sync lives in `<S5Shell>` client island.
 *
 * FEATURE-060
 */

import S5Shell from '@/components/solution-5sport/s5-shell';

export default function Sport5LandingPage() {
  return <S5Shell />;
}
